@AGENTS.md

# SELF-LEARNING

## Rules

1. **Read `tasks/lessons.md` at the start of every session, before doing anything else.**
2. **Apply every rule in `tasks/lessons.md` before touching any code.**
3. **After each correction from the user, immediately append an entry to `tasks/lessons.md`** using this format:

```
[YYYY-MM-DD] | what went wrong | rule to follow next time
```
