# Lessons learned

| Date | Ce qui s'est mal passé | Règle à suivre |
|------|------------------------|----------------|
| 2026-04-21 | Prompt rendu avec fences \`\`\` imbriquées, le bloc externe terminait sur le premier \`\`\`bash interne, cassant l'affichage | Pour un prompt/doc à copier-coller contenant des exemples code, ne JAMAIS wrapper le tout dans un code fence externe. Utiliser du markdown plat avec blocs \`\`\`lang inline. |
| 2026-04-21 | Tâche Claude.ai cloud lancée sans setup initial → agent démarré dans /home/user vide (repo pas cloné automatiquement par le GitHub connector) | Toujours inclure en première section du prompt cloud : \`cd /home/user && git clone https://github.com/{owner}/{repo}.git && cd {repo}\`. Aussi configurer les secrets (GOOGLE_PLACES_API_KEY etc.) dans la config Claude.ai, le .env.local n'est pas dans le repo. |
| 2026-04-21 | Daily run Bilbao/Montpellier : apostrophes non-échappées dans une string single-quoted en TypeScript, \`'Puppy'\` dans \`'...'\` a cassé le build Vercel. Bizarrement la version EN/ES utilisait bien \`\\'Puppy\\'\` mais pas FR. | Dans les strings single-quoted de cityContent.ts, TOUTES les apostrophes/guillemets simples à l'intérieur doivent être échappés \`\\'\`. Alternative plus safe : utiliser des backticks \`\`\`...\`\`\` pour les strings contenant des citations. Ajouter \`npx tsc --noEmit\` dans Step 11 du pipeline AVANT le commit, l'audit ne détecte pas les erreurs TS. |
| 2026-04-21 | Même run : \`export default cityContent\` SUPPRIMÉ accidentellement quand l'agent a ajouté Bilbao/Montpellier en fin de fichier. Build Vercel crash "Export default doesn't exist". Mon \`npx tsc --noEmit\` sur UN seul fichier ne détectait pas ça (le fichier seul compile OK, c'est l'import depuis page.tsx qui échoue). | Ne JAMAIS éditer lib/cityContent.ts en insérant juste avant \`}\` final sans vérifier que \`export default cityContent\` est toujours en dessous. Step 11 du pipeline doit inclure \`npm run build\` COMPLET (pas juste tsc --noEmit sur un fichier), qui détecte les imports cassés. Alternative plus rapide : \`grep -c "^export default cityContent" lib/cityContent.ts\` doit retourner 1. |
| 2026-04-21 | Villes dans lib/cityContent.ts ajoutées par la tâche nocturne TOUJOURS en fin de fichier (Bilbao + Montpellier après Athens), désordre total (paris, barcelona, rome... puis chaos) | Les nouvelles villes doivent être insérées alphabétiquement. Règle dans Step 8 : trouver le slug qui vient APRÈS alphabétiquement et insérer JUSTE AVANT lui. Le fichier a été trié via scripts /tmp/sort-citycontent.py. Si ça repart dans le désordre, re-trier. |
| 2026-04-21 | Noms de sights avec descripteurs anglais collés : "Écusson Medieval Quarter", "Parc Méric Off-Leash Area", "Berges du Lez Riverside Walk", le champ \`name\` n'est pas localisé, donc "Medieval Quarter" apparaît tel quel sur la page française. 11 noms cassés au total (Montpellier, Riga, Warsaw, Salzburg, Munich, Oslo, Tallinn, Bilbao). | (1) Le champ \`name\` des sights doit être un NOM PROPRE pur (locution dans la langue du pays ou neutre), jamais un descripteur anglais collé. (2) Si on veut ajouter une traduction, utiliser des parenthèses : "Stari Grad (Old Town Walls)" OK. (3) check-i18n.mjs a été étendu pour détecter automatiquement les descripteurs interdits (Off-Leash Area, Riverside Walk, Sculpture Park, Medieval Quarter, River Path, etc.), tourne à chaque \`npm run build\` donc bloque le déploiement si présent. |
| 2026-04-21 | Link "← Destinations" sur page destination visible mais NON CLIQUABLE, l'image de fond du hero (\`<Image fill>\` dans un \`div.absolute.inset-0\`) couvre tout le hero et capture les clics. Le div de contenu n'avait pas de \`relative z-10\` donc restait sous l'image en stacking. | Règle pour tout hero avec image de fond \`absolute\` : (1) le div overlay image doit avoir \`pointer-events-none\`, (2) le div de contenu doit avoir \`relative z-10\` pour passer au-dessus. Vérifier systématiquement sur toute nouvelle page avec hero image que les liens/boutons sont réellement cliquables. |
| 2026-04-21 | Hôtels ajoutés par la tâche nocturne (et par les commits manuels) n'étaient étiquetés que \`dog-friendly\` et jamais \`cat-friendly\`, même quand la politique Booking dit "Pets are allowed" (ce qui inclut les chats). Résultat : les 9 villes récentes (Marseille, Rotterdam, Bologna, Cologne, San Sebastián, Toulouse, London, Bilbao, Montpellier) avaient 0 à 3 hôtels cat-friendly sur 5-6. | Règle Step 9 : si la politique de l'hôtel dit "Pets are allowed" (ou "dogs and cats", ou "animaux"), ajouter AUSSI \`cat-friendly\` dans \`categories\`. N'ajouter \`dog-friendly\` seul QUE si la politique exclut explicitement les chats ("only dogs", "dogs only"). La plupart des hôtels Booking pet-friendly acceptent les 2. |
| 2026-04-21 | L'agent nocturne a ajouté Bilbao + Montpellier À LA FIN de TOUS les fichiers concernés : destinations.json (ordre 50/51), editorial.ts (3 blocs en/fr/es), hotels.json. Résultat visible : pages catégories (dog-friendly, cat-friendly) affichent Bilbao et Montpellier hors ordre alpha. CityContent.ts avait déjà été re-trié, mais les 3 autres fichiers non. | Règle Step 4, 5, 7, 8, 9 : TOUS les fichiers de données (destinations.json, hotels.json, editorial.ts, cityContent.ts) doivent être triés alphabétiquement après toute insertion. Scripts de tri disponibles : /tmp/sort-citycontent.py et /tmp/sort-editorial.py. Pour destinations.json et hotels.json : \`python3 -c "import json;d=json.load(open('F'));d.sort(key=lambda x:x['slug']);json.dump(d,open('F','w'),indent=2,ensure_ascii=False)"\`. Vérifier après chaque Step que le fichier est trié. |
| 2026-04-22 | Daily run Nantes+Frankfurt : Nantes a passé toute la pipeline, mais Frankfurt a crashé au Step 4 avec \`API Error: Claude's response exceeded the 32000 output token maximum\` en essayant d'écrire \`data/city-guides/frankfurt.json\` en UN SEUL \`Write\` call. Le JSON city-guide complet (8 sections × 3 places × 3 langues + tips/faqs) dépasse les 32k tokens d'output. Nantes committée localement MAIS PAS PUSHÉE au moment du crash, perte partielle du travail si l'humain n'était pas intervenu. | (1) Step 4 : NE JAMAIS écrire un city-guide JSON complet en un seul \`Write\`. Procédure : écrire la coque (\`{"city":"...","guides":{}}\`) via Write, puis ajouter CHAQUE section (restaurants, parks, transport, beaches, vets, tips, attractions, petsitting) via un \`Edit\` séparé, 1 section par tool call. (2) Règle pipeline : \`git push origin main\` IMMÉDIATEMENT après le commit de la ville 1 (dans Step 12), AVANT d'attaquer la ville 2. Max 2 cities sequential = 2 commits + 2 pushes, pas 1 push final. Si la ville 2 crashe, la ville 1 est déjà live. |
| 2026-04-24 | Templates FR/ES injectent \`{cat}\` au milieu de phrases qui supposent un adjectif anglais ("Pourquoi {dest} pour des hôtels {cat} ?" → "Pourquoi Antwerp pour des hôtels Chiens acceptés ?" en FR). Les noms de catégories FR/ES ("Chiens acceptés", "Hôtels de luxe", "À proximité des parcs", "Sans supplément animaux") sont des locutions qui ne fonctionnent pas comme adjectifs avant "hôtels". En plus, \`dest.name\` (toujours en anglais dans destinations.json) est utilisé tel quel dans les meta titles/descriptions FR/ES → "Antwerp" au lieu de "Anvers", "Belgium" au lieu de "Belgique". | (1) Templates avec \`{cat}\` dans dictionaries/{fr,es,en}.json : utiliser un séparateur (, ou :) au lieu d'imbriquer la catégorie comme adjectif. Ex : \`Hôtels à {dest}, {cat}\` au lieu de \`Meilleurs hôtels {cat} à {dest}\`. Pour le \`whyTitle\` : drop le \`{cat}\` complètement, mettre \`Pourquoi choisir {dest} avec votre animal ?\` (la catégorie est déjà dans les bullets). (2) Toujours utiliser \`getLocalizedCityName(slug, name, locale)\` pour le nom de ville et \`getLocalizedCountryName(country, locale)\` pour le pays dans tout texte FR/ES rendu (titles, meta descriptions, body). Les schémas JSON-LD et alt-text peuvent rester en anglais. (3) Avant de commit un template avec \`{var}\`, simuler le rendu avec les 6 catégories réelles (Chiens acceptés, Chats acceptés, À proximité des parcs, Hôtels de luxe, Accès à la plage, Sans supplément animaux), si une variante produit une duplication ("hôtels Hôtels") ou une formule cassée, retravailler. |
| 2026-04-24 | Section \`petsitting\` avait 22+ cartes avec des photos absurdes : Google Places matchait "Rover Strasbourg/Naples/..." sur les concessionnaires **Land Rover** locaux → une concession automobile s'affichait à la place d'une plateforme de pet-sitting. Pawshake/Holidog même problème (entreprises différentes au même nom). Aussi : "Berges de l'Ill en Petite France" (Strasbourg) → Google Places n'a rien trouvé (zone diffuse, pas un POI précis) → photo manquante visible sur le front. | (1) Ne JAMAIS fetcher Google Places photos pour la section \`petsitting\`, ce sont des plateformes en ligne (Pawshake, Rover, Holidog) sans lieu physique. Le script \`fetch-city-guide-photos.mjs\` skip désormais cette section entièrement (le front gère \`!place.photo\` avec un header sans image). (2) Pour les zones diffuses (berges, promenades, quartiers non-POI), ajouter dans la prochaine itération un fallback : si \`No photo found\`, retry avec une requête ciblée sur un POI précis de la zone (ex: "Berges de l'Ill" → "Ponts Couverts Strasbourg"). Pour l'instant, fix manuel si détecté. |
| 2026-04-26 | "PLUS DE DESTINATIONS EN SPAIN" et plein de fuites d'anglais sur le site (countries/page.tsx, OG images, DestinationsGrid, TopHotels, generateXXX dans editorial.ts). Aussi, tentative de remplacer **dog-friendly** par "accueillant les chiens" en FR a cassé la grammaire dans les superlatifs ("les villes les plus accueillant les chiens", faux, faut "accueillantes pour"). En ES, "que admiten perros" cassait les relatives ("la capital que admiten perros de Europa"). | (1) **Audit i18n parallèle** : lancer 3 agents Explore en parallèle pour scanner (a) static text leaks, (b) content data files JSON/TS, (c) dictionaries + components. Plus rapide qu'un seul agent et ratisse plus large. (2) **Régle stricte** : pour TOUT texte rendu en FR/ES (homepage components, pages, OG images), passer par \`getLocalizedCityName\` et \`getLocalizedCountryName\`. Inclus les composants \`components/home/*\` et les fichiers \`opengraph-image.tsx\`. (3) **Anglicismes acceptés** : \`dog-friendly\` et \`pet-friendly\` ne se traduisent PAS, leur remplacement littéral casse la grammaire FR/ES. \`off-leash\` → \`sans laisse\` / \`sin correa\` et \`on-leash\` → \`en laisse\` / \`con correa\` SE traduisent (compléments propres, pas adjectifs). (4) **Avant tout sed/regex bulk-replace** sur du contenu, tester sur 5-10 occurrences à la main avec différents contextes grammaticaux (comparatif, superlatif, attribut, complément), si UNE produit du charabia, abandonner et garder l'anglicisme. |
| 2026-04-27 | Daily run The Hague + Brighton : pipeline propre du premier coup. Ce qui a marché, à reproduire : (a) backticks par défaut pour TOUTES les nouvelles entrées cityContent.ts contenant des apostrophes, zéro échappement à se rappeler ; (b) JSON ville écrit en 1 Write + 7 Edit séparés (un par section), aucun timeout 32k tokens ; (c) tri alpha vérifié immédiatement après chaque insertion (destinations.json, hotels.json triés par script, editorial.ts × 3 blocs et cityContent.ts insérés à la position alpha exacte) ; (d) commit + push immédiat après ville 1 avant d'attaquer ville 2. | Continuer à appliquer strictement v5 du pipeline. Les 5 pièges historiques (apostrophes, export default, sights anglais, ordre alpha, hôtels cat-friendly oubliés) sont tous évités quand on : (1) backticks systématiques pour cityContent.ts, (2) `grep -c "^export default cityContent" lib/cityContent.ts` = 1 avant build, (3) `name` des sights = nom propre pur, descripteurs en `desc`, (4) re-tri Python explicite après toute insertion JSON, (5) `cat-friendly` ajouté par défaut sauf "only dogs" explicite dans la politique Booking. |
| 2026-04-29 | Daily run Lille + Zagreb : pipeline propre, 80→82 villes parity-compliant. Découverte utile : un orphan city-guide (lille.json existait depuis avant mais sans destination entry, détecté par `audit-destinations.mjs` dans la section "Orphan city-guide files") permet de "fast-track" une ville en évitant le travail de recherche/rédaction des 7 sections déjà présentes, il suffisait d'ajouter la section `petsitting` manquante puis de wirer la ville (destinations.json, editorial.ts, cityContent.ts, hotels.json). | Au démarrage de chaque run, vérifier `audit-destinations.mjs | grep -A1 Orphan`, si un fichier orphan a 7-8 sections complètes, le compléter coûte moitié moins de tool calls qu'une nouvelle ville. Si le guide orphan n'a qu'1-2 sections (cas Stuttgart au 2026-04-29), le considérer comme stale et le sauter, refaire à zéro est plus rapide que rattraper du contenu incohérent. |
| 2026-05-04 | Daily run Galway (orphan fast-track) + Cork (full pipeline) : Cork build a crashé au prerender de `/destinations/cork/petsitting` avec `TypeError: a.languages.join is not a function`. La section `petsitting` attend `languages: string[]` (array, ex: `["English","French"]`) mais j'avais écrit une string `"English (a few French/Spanish/German speakers in Cork)"`. Le check-i18n et la validation JSON ne détectent pas ce mismatch de type, seul le prerender Next.js le voit. | Dans tous les city-guides, le champ `languages` de la section `petsitting` doit être un **array de strings** (ex: `["English","Irish","French"]`), jamais une string libre. Si tu veux qualifier la disponibilité linguistique, mets le détail dans le `description` ou un `tip`, pas dans `languages`. Vérification rapide avant build : `python3 -c "import json,glob; [print(f) for f in glob.glob('data/city-guides/*.json') for s in [json.load(open(f))['guides'].get('petsitting',{}).get('places',[])] for p in s if not isinstance(p.get('languages',[]), list)]"`. Step 11 du pipeline doit inclure `npm run build` complet (pas juste i18n check), le prerender détecte les type mismatches que la validation JSON ignore. |
| 2026-05-07 | Daily run Basel : au démarrage, `git status` était clean mais `data/destinations.json`, `data/hotels.json`, `lib/cityContent.ts`, `lib/editorial.ts` contenaient déjà des modifs Faro non-commitées (et un `data/city-guides/faro.json` + 18 photos place untracked) d'un run précédent abandonné. L'audit listait Faro comme parity-compliant (les data files étaient là), mais Faro n'était pas sur origin. Ajouter Basel par-dessus a entraîné un commit auto-généré (par un hook ou un autre process) qui a embarqué les deux villes sous un message "feat: add Faro, full parity, 105/105 destinations", message trompeur mais shipping correct. | (1) Au début de chaque run, vérifier non seulement `git status` mais aussi `git diff --stat HEAD origin/main` ET `git diff` sur les 4 fichiers de données : si une autre ville est déjà modifiée localement, c'est un orphan-in-progress d'un run précédent qu'il faut soit finir (audit clean + build pass) et committer séparément AVANT d'ajouter sa ville, soit reset proprement. (2) Si un auto-commit hook réécrit ton message, ne pas s'en inquiéter, vérifier que le `git show HEAD --stat` contient bien tous les fichiers de ta ville, et que `git push` confirme le ship. (3) Faro avait été ajouté à destinations.json/cityContent.ts/editorial.ts/hotels.json mais le `git commit` final n'avait pas eu lieu, vraisemblablement crash du run avant Step 12. Récupérer ce travail abouti coûte zéro tool call ; c'est un gain net. |
| 2026-05-09 | Daily run Liverpool + Düsseldorf : pipeline propre, 110→112 villes parity-compliant. Découverte mineure : `fetch-city-guide-photos.mjs` peut échouer sur une seule photo avec `VipsJpeg: Invalid SOS parameters for sequential JPEG` (Brauerei Im Füchschen, Düsseldorf), la photo a été trouvée par Google Places mais sharp n'a pas su la décoder. Le front gère `!place.photo` proprement (header sans image), donc non-bloquant. Pas la peine de re-runner ; passer à la suite. Aussi : pour Liverpool/UK, ajouter un rappel post-Brexit (Animal Health Certificate au lieu du passeport européen) dans la section tips ET dans cityContent.practicalInfo, c'est une info que le voyageur EU ne s'attend pas à devoir vérifier. Pour Düsseldorf, mentionner les Listenhunde restrictions de la NRW Landeshundegesetz dans petTips (chiens de catégories 1/2 réglementés). | (1) Si une seule photo de place échoue avec une erreur sharp/VipsJpeg, ne pas relancer 3 fois, la couverture sur 14/15 ou 13/15 places suffit, le front gère gracieusement les missing photos. (2) Pour les villes UK : toujours mentionner la paperasse post-Brexit (AHC + microchip + rabies) à au moins deux endroits dans le contenu, c'est l'info pratique la plus différenciante pour un visiteur EU. (3) Pour les villes allemandes en NRW (Düsseldorf, Cologne, Bonn, Münster, Aachen) : mentionner les Listenhunde dans petTips ou practicalInfo, la liste varie par Land et c'est un blocage potentiel à l'arrivée pour certaines races. |
| 2026-05-15 | Daily run Lucerne + Bremen : pipeline propre, 122 → 124 villes parity-compliant. Aucun crash, build PASS du premier coup pour les deux villes, photos 18+17/18 (un seul Tiernotruf service-rota Bremen sans photo, gracefully géré par le front), tri alpha vérifié dans les 4 fichiers à chaque insertion. Découverte mineure : pour les services qui sont en réalité un *Notdienst tournant* (numéro central qui redirige selon le tour), Google Places ne trouve rien, ne pas perdre de temps à chercher une photo, le front gère `!place.photo` avec un header sans image. | Pour toute section vétérinaire incluant un service de garde régional purement téléphonique (pas un cabinet physique fixe), prévoir d'avance que Google Places échouera et accepter l'absence de photo comme normale. Inclure quand même l'entrée, la valeur informative est le numéro et les horaires, pas l'image. Pour Lucerne et Bremen, la pipeline v5 reste fiable : (1) backticks systématiques pour cityContent.ts, (2) JSON ville en 1 Write + 7 Edits séquentiels, (3) tri alpha Python explicite après chaque insertion, (4) cat-friendly par défaut sur tous les hôtels Booking pet-friendly sauf "only dogs", (5) commit + push immédiat après ville 1 avant d'attaquer ville 2. |
| 2026-05-12 | 9 jours de builds Vercel bloqués depuis Évora (commit eef09cd). Les logs Vercel montraient « Build Completed » puis « Deploying outputs… » puis SILENCE, pas de message d'erreur. Le déploiement échouait pendant l'upload des assets vers le CDN. **2 causes cumulées**: (1) 42 images hero non compressées de 8-40 MB chacune (raw 4422x2328 px JPEGs, total 391 MB rien que pour destinations/), la commande `fetch-destination-photos.mjs` resize bien à 1600x900 mais ces 42 images dataient d'AVANT que le script soit utilisé ; (2) **85 192 fichiers dans .next** (combo pages + hotel pages prerendered eagerly), au-dessus du seuil silencieux Vercel pour l'upload CDN. | **(1) Audit régulier `du -sh public/images/destinations/`**, si > 100 MB, run `node -e "const sharp=require('sharp');const fs=require('fs/promises');const path=require('path');(async()=>{for(const f of await fs.readdir('public/images/destinations')){const fp=path.join('public/images/destinations',f);const s=await fs.stat(fp);if(s.size<500*1024)continue;const buf=await fs.readFile(fp);const out=await sharp(buf).resize(1600,900,{fit:'cover'}).jpeg({quality:82,progressive:true,mozjpeg:true}).toBuffer();await fs.writeFile(fp,out)}})()"` pour compresser tout ce qui dépasse 500 KB. **(2) Pour toute route SSG à très haute cardinalité (>1000 routes × 3 locales), utiliser ISR à la demande** au lieu de prerender complet : `export const dynamicParams = true; export const revalidate = 86400; export async function generateStaticParams() { return [] }`. Pages restent en sitemap, Google les indexe pareil, mais elles sont rendues serverside à la demande puis cachées 24h. Combo pages (`/[locale]/[destination]/[category]`) et `[locale]/hotels/[slug]` sont en ISR depuis ce fix. **(3) Avant tout push, compter `find .next -type f | wc -l`, si > 60 000, c'est un risque pour Vercel deploy.** **(4) Lire les logs Vercel JUSQU'À LA FIN, si le log « Deploying outputs… » se coupe sans suite, c'est un fail silencieux. Pas la peine de chercher l'erreur build-time, c'est l'upload qui meurt.** |
| 2026-05-16 | Daily run Cascais : un AUTRE processus (autre instance Claude / autre tâche planifiée daily-city-guides tournant en parallèle) a auto-commité Palermo + Pamplona + FAQPage schema au milieu de mon run (commit `c5c3b1c`, message "Cleanup: removed phantom cascais destination entry"). Cet auto-commit a INCLUS mon `data/city-guides/cascais.json` et mon hero `public/images/destinations/cascais.jpg` (qui étaient untracked) MAIS a SUPPRIMÉ mon entrée `cascais` de `data/destinations.json` parce qu'elle était partielle au moment du snapshot. Mes modifs intermédiaires sur `lib/cityContent.ts`, `lib/editorial.ts` et `data/hotels.json` ont survécu dans le working tree (non commitées). Résultat : audit revenu à 128/128 sans cascais, et j'ai dû re-faire l'append dans `destinations.json` puis continuer. Aussi : le message de commit de l'autre process mentionnait "FAQPage schema on tips pages" mais ne touchait pas `app/[locale]/destinations/[slug]/[guide]/page.tsx`, le code de la feature était dans MON stash (non commité), pas dans le sien. | **(1) Si tu travailles sur la tâche `daily-city-guides`, vérifie au démarrage `git log --oneline -5` puis re-vérifie après chaque ÉTAPE majeure (Step 5, Step 7, Step 8, Step 9). Si `HEAD` change pendant ton run, un autre process est en parallèle.** **(2) Quand un auto-commit te dépouille d'une modif partielle, ne pas reset destinations.json, re-appliquer l'append (Step 5) avec exactement le même payload. Le reste de tes edits sur `cityContent.ts` / `editorial.ts` / `hotels.json` reste dans le working tree (git status le confirme).** **(3) Ne pas faire confiance aux messages d'auto-commit générés par un autre LLM : `git show <sha> --stat` est la source de vérité, vérifie les fichiers RÉELLEMENT modifiés avant de croire la description. Le message peut prétendre "FAQPage schema added" alors que le diff ne touche pas la page concernée.** **(4) Quand tu détectes ce conflit en cours de run, le moins coûteux est de FINIR ta propre ville (re-append + photos + commit + push) puis d'annexer dans le rapport "shipped X, auto-commit du parallèle a aussi shippé Y+Z". Pas la peine de revert l'autre.** |
| 2026-05-17 | Daily run Catania + Tarragona : pipeline propre, 131 → 133 villes parity-compliant. Premier Edit de la coque `data/city-guides/catania.json` a échoué avec "String to replace not found in file" parce que le fichier nouvellement créé par Write se termine par `}\n` mais mon `old_string` capturait `}\n}\n` avec un retour à la ligne final invisible que l'éditeur traitait différemment. Solution : faire un Read juste avant la 1ère Edit après Write pour aligner exactement les bornes, ou simplifier l'old_string aux deux dernières lignes uniquement. Aussi : Catania et Tarragona ont chacune 4 entrées dans `lib/editorial.ts` (les 4 blocs locale `en/fr/es/pt` du seul Record `destContextByLocale`), pas dans 5 Records comme le suggère encore un commentaire du pipeline doc. Les 4 autres Records (`catIntrosByLocale`, `catTipsByLocale`, `bestSeasonByLocale`, `testimonialsByLocale`) sont keyed par catégorie (`dog-friendly`, `cat-friendly`, …), PAS par ville, donc rien à ajouter quand on insère une nouvelle ville. | (1) Après tout `Write` d'un fichier neuf, faire un Read explicite du fichier avant le 1er Edit pour aligner les bornes ; si l'Edit échoue avec « String to replace not found », réduire l'old_string aux 2–3 dernières lignes du fichier, sans le `\n` terminal. (2) Pour `lib/editorial.ts` v6 : SEUL `destContextByLocale` est keyed par ville. Les 4 autres Records (`catIntros`, `catTips`, `bestSeason`, `testimonials`) sont keyed par catégorie, IGNORE-LES en Step 7. Le `grep -c "    {slug}: {" lib/editorial.ts` doit retourner 4 (4 blocs locale), pas 5. (3) Step 7 du pipeline doc dit encore « 5 *ByLocale Records » — c'est obsolète, le compteur réel est 1 Record city-keyed × 4 locales = 4 insertions. Mettre à jour le pipeline doc à la prochaine occasion.|
| 2026-05-17 | Daily run Cardiff : crash de prerender `/pt/destinations/cardiff/vets` avec `TypeError: a.languages.join is not a function` parce que j'avais écrit `languages: "English, basic Welsh signage..."` (string longue) dans la section `vets` au lieu d'un array. Exactement la même erreur que Cork au 2026-05-04, le `languages` du schema vets attend un `string[]`. Fix immédiat : convertir en `["English","Welsh"]` puis rerun `npm run build`, qui a pass au 2ᵉ essai. | (1) `languages` est TOUJOURS un array (tableau de strings courts) dans `vets` ET `petsitting`, jamais une phrase longue. Si tu veux qualifier la disponibilité linguistique, mets le détail dans `description` ou dans un `tip` séparé. (2) Vérification 1-liner avant build : `python3 -c "import json,glob; [print(f,p['name']) for f in glob.glob('data/city-guides/*.json') for sec in ['vets','petsitting'] for p in json.load(open(f))['guides'].get(sec,{}).get('places',[]) if not isinstance(p.get('languages',[]),list)]"` doit ne rien imprimer. (3) La lesson Cork de 2026-05-04 le couvrait déjà mais seulement pour `petsitting`, mettre à jour mentalement : la règle s'applique aussi à `vets` (et à toute future section qui accepterait `languages`).|
| 2026-05-18 | Daily run Bonn + Malmö : pipeline propre du premier coup, 135 → 137 villes parity-compliant. Bonn a fait usage de la lesson 2026-05-09 sur NRW Listenhunde (mention dans tips ET cityContent.practicalInfo), et Malmö a inclus la règle suédoise du kopplingstvång saisonnier (1ᵉʳ mars–20 août) + l'interdiction de baignade canine 15 mars–30 septembre. Photos 18+18/18 chacune, hôtels 5+5/5 avec cat-friendly par défaut sauf Kameha Grand Bonn et Clarion Malmö Live qui interdisent explicitement les chats. Aucune correction nécessaire. | Continuer à appliquer la v5 stricte : (1) backticks systématiques pour cityContent.ts, (2) JSON ville en 1 Write + 7 Edits, (3) tri alpha Python explicite après chaque insertion JSON, (4) cat-friendly par défaut sauf "only dogs" ou "no cats" explicite Booking, (5) commit + push immédiat après ville 1 avant la ville 2. Pour les villes scandinaves (Suède/Norvège/Finlande/Danemark), toujours mentionner les règles tapeworm (échinocoque) à l'arrivée ainsi que le calendrier de leash law national, c'est l'info pratique la plus différenciante. Pour les villes NRW allemandes (Bonn, Cologne, Düsseldorf, Bonn-area), toujours mentionner les catégories Listenhunde 1+2 dans tips et practicalInfo.|
| 2026-05-20 | Daily run Uppsala + Linz : pipeline propre du 1er coup, 142 → 144 villes parity-compliant. Piège évité de justesse au début d'Uppsala : j'ai écrit le JSON city-guide avec des backticks (\\`...\\`) au lieu de double quotes — invalide pour JSON. Détecté immédiatement par `python3 -c "json.load(...)"`. Fix : sed-replace tous les backticks par double quotes, OK puisque les strings ne contenaient pas de double quotes embarquées. Lesson : **la règle "backticks systématiques" s'applique UNIQUEMENT à lib/cityContent.ts (TypeScript), JAMAIS aux data/city-guides/*.json (JSON strict). Pour JSON, double quotes obligatoires, apostrophes ne nécessitent PAS d'échappement dans JSON. Vérifier avec `python3 -c "import json; json.load(open('data/city-guides/X.json'))"` immédiatement après chaque Edit sur un city-guide.** Pour Linz spécifiquement : (a) la Maulkorbpflicht sur Linz Linien est strictement contrôlée avec amende sur place 105 €, info pratique la plus différenciante à mentionner dans tips ET cityContent.petTips ; (b) les deux lacs publics de baignade (Pleschinger See et Pichlinger See) interdisent strictement les chiens dans les zones de baignade pour hygiène — la Hundefreilaufzone Urfahr/Donau sur le Danube est la seule option de baignade canine ; (c) le service vétérinaire 24h/24 le plus proche n'est PAS à Linz mais à Tierklinik Sattledt 35 km à l'ouest sur l'A1, c'est le relais officiel auquel la municipalité de Linz redirige les urgences nocturnes. | (1) Pour data/city-guides/*.json, TOUJOURS double quotes, JAMAIS backticks (JSON strict). Pour lib/cityContent.ts, TOUJOURS backticks (TypeScript, évite l'échappement des apostrophes). (2) Pour les villes autrichiennes (Vienne, Salzbourg, Innsbruck, Graz, Linz), toujours mentionner la Maulkorbpflicht sur le réseau de transport public local — c'est l'info pratique la plus différenciante et celle qui surprend le plus les voyageurs européens, notamment ceux venant de France, d'Espagne ou du Royaume-Uni où aucun équivalent n'existe. (3) Pour les villes avec un service vétérinaire 24h/24 hors-ville officiellement référencé par la municipalité (Linz → Sattledt), inclure le numéro à la fois dans le city-guide vets section ET dans cityContent.petTips. |
| 2026-05-23 | Ultrareview a découvert 14 hôtels Brno/Stavanger/Trondheim avec `priceFrom` en devise locale non convertie (CZK ×25, NOK ×11.5) → affichés à €2200-€3800/nuit, totalement faux et anti-conversion. Toutes shippées par des agents qui n'ont pas converti la devise Booking en EUR avant d'écrire `priceFrom`. | Tout `priceFrom` doit être en EUR. Avant d'écrire un hôtel dans hotels.json, **convertir explicitement** : si la page Booking affiche `1 850 Kč` ou `1 850 NOK`, calculer en EUR (1 EUR ≈ 25 CZK, 1 EUR ≈ 11.5 NOK) puis stocker la valeur EUR. Le champ `currency: "EUR"` ne suffit pas, c'est la valeur numérique qui doit déjà être en EUR car le rendu hardcode `€` partout. Pour la prochaine ville en CZ/NO/SE/PL, dégager les conversions devise *avant* l'Edit hotels.json. |
| 2026-05-23 | Hreflang alternates manquaient pt: sur 7 pages (destinations, destinations/[slug], about, guides, by-month, categories, categories/[slug]) malgré le shipping de PT depuis plusieurs semaines. Ces pages ont en/fr/es + x-default dans `alternates.languages` mais pas pt → Google ne sait pas que les versions /pt/* existent → indexation PT bloquée sur ces routes. | Toute nouvelle route avec `alternates.languages` doit lister les 4 locales (en/fr/es/pt) + x-default. Pattern obligatoire : `{ en: ${SITE_URL}/en/path, fr: ${SITE_URL}/fr/path, es: ${SITE_URL}/es/path, pt: ${SITE_URL}/pt/path, 'x-default': ${SITE_URL}/en/path }`. Ajouter un check automatique au build : grep des fichiers `app/[locale]/**/page.tsx` ayant `alternates.languages` sans `pt:`. |
| 2026-05-25 | Brittany road-trip guide shippé avec strings hardcodées EN ("Day 1/2-3", "Check availability →", "Browse all pet-friendly hotels in X →") qui apparaissaient telles quelles dans les vues /fr, /es, /pt. Le COPY object localisait les blocs longs (intro, FAQ, etc.) mais oubliait les micro-labels UI. | Sur toute nouvelle page multilingue, AUCUNE string visible à l'utilisateur ne doit être en dur dans le JSX. Toutes les micro-strings (labels CTAs, jours, "voir plus", chips, badges) doivent vivre dans le COPY[locale] dict. Faire un grep final avant commit : `grep -E "(>[A-Z][a-z]+ →|Day [0-9]|Check |Browse |See |View )" path/to/page.tsx` — toute occurrence dans un literal JSX = bug i18n. |
| 2026-05-26 | Audit a révélé que le template `/destinations/[slug]/[guide]/page.tsx` (qui sert TOUTES les pages /beaches /restaurants /tips /parks /transport /vets /attractions /petsitting — soit ~1000 landing SEO) n'avait AUCUN CTA hôtel inline. Le seul lien hôtel était un bloc tout en bas après tips + FAQ. Clarity confirmait : Zurich/beaches 34 sessions → 0 outbound clic. Pendant des semaines on a ajouté des CTAs sur `/guides/*` (articles éditoriaux, faible trafic) sans toucher le template à fort trafic. | Quand on shippe un composant de conversion (NearbyHotelCard, TopHotelsStrip, StickyHotelCTA), faire un grep `grep -rln NomDuComposant app/` et CROISER avec la liste des routes à fort trafic (GA4 top 30 pages). Si le composant n'apparaît pas dans le template d'une route top-30, c'est un bug de déploiement. Procédure : (1) lister les routes top trafic via GA4/Clarity, (2) pour chaque template servant ces routes, vérifier l'import du composant de conversion, (3) si absent, c'est P0. |
| 2026-05-26 | Site illisible en iOS Safari dark mode : `globals.css` honorait `prefers-color-scheme: dark` (background #0a0a0a) mais TOUTE l'UI utilise des couleurs hardcodées light (`text-gray-900`, `bg-white`). Résultat : titres de section et chevrons FAQ devenaient noir sur noir, sections entières invisibles. | Pour un site SEO public sans toggle de thème, forcer `color-scheme: light` dans `:root` et NE PAS ajouter de bloc `@media (prefers-color-scheme: dark)`. Si on veut un jour un vrai dark mode, ce sera un projet complet : ajouter `dark:` variants Tailwind partout, refaire tous les composants. |
| 2026-05-26 | Pages /destinations/{slug}/tips affichaient des photos Google Places non pertinentes : pour des concept-cards "Currency & cost" / "Best season", le script `fetch-city-guide-photos.mjs` retournait des logos de marques (ex : logo Ria Money Transfer pour "Currency & cost"). De plus, les noms des cards "tips" n'étaient jamais traduits → "Currency & cost" en gros sur une page FR. | (1) Sur les pages `tips`, ne JAMAIS afficher `place.photo` (concept cards, pas vrais lieux). (2) Maintenir une `TIP_NAME_I18N` dans le template `[guide]/page.tsx` qui localise les noms de tip récurrents (Currency & cost, Best season, Languages, Heat & paw safety, Post-Brexit/UK paperwork…). (3) Le script de fetch photos ne devrait JAMAIS scrapper Google Places pour la section tips — l'ajouter à l'exclusion du script. |
| 2026-05-27 | Ajouté Doggyhut Medium remorque vélo via ASIN B007N7DQ2C trouvé en web search ; ASIN était silencieusement réattribué côté Amazon à un piège anti-moustique. Carte affichait l'image d'un zapper sur une page sacs de transport chien. Pareil avec B06Y4CCPRC (WoofWoof gamelle) signalée par l'utilisateur "n'est plus le bon". | Pour chaque ASIN Amazon ajouté au registry, faire un check minimum : (1) ouvrir manuellement `https://www.amazon.fr/dp/{ASIN}` dans le navigateur ET vérifier que titre/photo correspondent à ce qu'on prétend, OU (2) WebFetch la page produit avec prompt "donne le titre du produit affiché" et matcher avec notre `name`. Amazon ré-attribue régulièrement les ASINs de produits discontinués à de nouveaux items sans rapport. Le script `fetch-amazon-images.mjs` n'a aucun moyen de détecter ce drift — il ramène fidèlement l'image, mais du mauvais produit. |
| 2026-05-28 | Daily run Santiago de Compostela (orphan fast-track) + Vigo (full pipeline) : 166 → 168 villes parity-compliant. Santiago : guide JSON déjà complet (8 sections × 4 locales, 14/14 photos déjà nommées) trouvé via `audit-destinations.mjs | grep Orphan`, ne restait qu'à câbler destinations.json + editorial.ts × 4 + cityContent.ts + 5 hôtels + hero. Coût ≈ moitié d'une ville full pipeline. Vigo : full pipeline propre, photos 5/5 hôtels + 14/14 places, build PASS au 1er essai. Confirmation que la stratégie « orphan-first » au démarrage du run vaut le coup d'œil même quand l'audit signale juste 1 orphan. Aussi : `booking.com` retourne HTML vide via WebFetch (bot-block), il faut reposer entièrement sur WebSearch summary pour vérifier les pet policies, et croiser plusieurs sources (Hotels.com, KAYAK, Expedia, BringFido) quand la réponse est ambiguë (cas Parador Hostal dos Reis Católicos). Pour Vigo, point différenciant rare : interdiction TOTALE des chiens dans le Parc National des îles Cíes, à documenter dans 3 emplacements (transport.rules, beaches.intro, tips.sections + cityContent.sights/petTips) parce que c'est l'erreur n°1 du touriste arrivant à Vigo avec un chien. | (1) `audit-destinations.mjs | grep -A1 Orphan` en début de run est toujours rentable, fast-track d'une ville orphane = ~50% du coût d'une ville full pipeline. (2) Pour pet policies, la chaîne fiable est : WebSearch initial → WebFetch (échoue souvent côté booking.com) → recoupement multi-sources (Hotels.com + KAYAK + Expedia + BringFido). Si 2 sources sur 3 sont contradictoires, sauter l'hôtel et en chercher un autre. (3) Quand une ville a une règle anti-chiens spectaculaire dans son périmètre touristique (Cíes pour Vigo, ordonnance A Coruña pour Santiago, Maulkorbpflicht pour Linz), la mentionner dans 3+ emplacements du contenu : section pertinente du city-guide, tips, cityContent.petTips. C'est l'info qui sauve un voyage planté.|
| 2026-05-28 | Le hub Guides liait vers `/guides/dog-friendly-europe-by-month` (sans segment mois) alors que la route est `[month]/page.tsx` avec mois obligatoire → 404 « Page introuvable ». La page mensuelle marchait (january/february...) mais l'URL nue depuis le hub plantait. | Pour toute route dynamique `[param]/page.tsx` qui n'a PAS de `page.tsx` index frère, vérifier qu'AUCUN lien (nav, hub, footer, sitemap) ne pointe vers le chemin nu sans le param. Si un lien nu est voulu (hub), créer une `page.tsx` index qui `redirect()` vers une valeur par défaut sensée (ici : le mois courant). Grep avant ship : `grep -rn "slug-de-la-route" app/ | grep href` et confirmer que chaque href a bien le segment dynamique. |
| 2026-05-31 | Re-introduced 36 em-dashes "—" dans la page honeymoon-with-pet/page.tsx en l'écrivant from scratch, alors que la lesson 2026-05-27 dit explicitement "no em-dashes anywhere". L'utilisateur n'a pas vu (j'ai détecté moi-même avant ship) mais la règle a été cassée pendant ~5 min entre commit e78eaf8 et fix 7dc352c. | AVANT le commit de toute nouvelle page tsx que je crée from scratch, exécuter SYSTÉMATIQUEMENT : `grep -c "—" path/to/new-page.tsx` — doit retourner 0. Si non-zéro, sed -i '' 's/—/-/g' avant commit. À intégrer comme étape mentale obligatoire après chaque Write d'une nouvelle page : tsc + em-dash check + commit, dans cet ordre. |
| 2026-06-02 | Daily run Neringa (orphan fast-track, finition + ship). Working tree au démarrage contenait un orphan Neringa partiel (destinations.json + editorial.ts×4 + neringa.json déjà faits par un run précédent abandonné) ET la complétion cityContent.ts de Klaipeda (dont le commit f1a1b0a avait DÉLIBÉRÉMENT déféré l'entrée cityContent, « degrades gracefully via le guard cityContent[slug] && … »). L'audit listait Neringa en **erreur bloquante** (heroImage manquant) mais 0 hotels seulement en **warning non-bloquant** — donc le seul blocker réel était le hero. 3 vrais pièges rencontrés : (1) **« Kint Perlas » est à Kintai (continent), PAS à Neringa/Nida** — une recherche « pet-friendly Neringa » a remonté une propriété hors-spit ; (2) `fetch-hotel-photos-google.mjs` construit la query `${name} hotel ${cityName}` → pour une micro-municipalité comme Neringa, « Kastytis hotel Neringa » a matché une propriété littéralement nommée « Neringa Hotel », donnant la **même photo en double** sur Kastytis ET Guboja (md5 identiques) ; (3) un **process PT-backfill parallèle** avait modifié klaipeda.json avec du PT Spanglish de mauvaise qualité (« junto al rio », « cerveza artesanal », « puerta ») dans mon working tree. | (1) **Vérifier la localisation géographique réelle de chaque hôtel** avant de l'ajouter : une recherche « pet-friendly {ville} » peut remonter des propriétés de communes voisines (Kintai vs Neringa). Croiser le nom de la commune/village exact (ici Nida/Juodkrante/Preila/Pervalka) dans l'URL Booking et la description. (2) Pour les micro-municipalités / stations balnéaires, le script hotel-photos mis-matche : re-fetcher les photos mal matchées avec une query plus précise (`{name} guest house {village} {region}` au lieu de `{name} hotel {municipalité}`). **Vérifier les doublons : `md5 -q public/images/hotels/{slug}-*.jpg`** — deux md5 identiques = même photo sur 2 cards = re-fetch obligatoire. Rappel : `HotelCard.tsx` rend `<Image>` SANS check d'existence (contrairement à TopHotelsStrip/NearbyHotelCard qui font `existsSync`), donc on ne peut PAS supprimer une photo hôtel pour la dégrader gracieusement — il FAUT un fichier correct. (3) **0 hotels = warning non-bloquant, heroImage manquant = erreur bloquante** dans audit-destinations.mjs : pour « shippable » au sens 0 erreur il suffit du hero ; pour 0 warning il faut aussi les 5 hôtels. (4) Quand un process parallèle a sali un fichier hors-scope (klaipeda.json PT-backfill), **stager chirurgicalement uniquement mes fichiers** (`git add` fichier-par-fichier, jamais `git add data/ lib/` en bloc comme le suggère Step 12) pour ne pas embarquer son travail. 6 villes restent en warning 0-hotels (bayonne, brest, getxo, klaipeda, vannes, vitoria-gasteiz) — backlog. |
| 2026-06-04 | Poznań shippé avec `languages: "Polish, English"` (string) sur ses 3 vets au lieu de `["Polish", "English"]` (array). Le template `[guide]/page.tsx` fait `place.languages.join(', ')` qui throw sur une string. Build Vercel CRASH au moment du static export pour `/fr/destinations/poznan/vets`, déploiement entier rollback. Détecté car le user m'a envoyé le log de build. | Sur tout city-guide JSON shippé, lancer ce check AVANT commit : `python3 -c "import json,glob; [print(f'BAD {f}: {p[\"name\"]} - {repr(p[\"languages\"])}') for f in glob.glob('data/city-guides/*.json') for sec in json.load(open(f))['guides'].values() for p in sec.get('places',[]) if 'languages' in p and not isinstance(p['languages'], list)]"`. Doit ne rien afficher. À ajouter au Step 9 du pipeline doc. La même règle s'applique probablement à d'autres champs array-typed (`categories`, `tags`) — préventivement valider que tout array-look-like est bien `[]` pas une string. |
| 2026-06-05 | Daily run : orphan Tromso (Norvège) trouvé dans le working tree au démarrage (tromso.json untracked + destinations.json/cityContent.ts/editorial.ts modifiés non-commités par un run précédent abandonné). Le SEUL blocker réel était l'image hero manquante (erreur bloquante audit) ; 0 hotels n'était qu'un warning. Fast-track : fetch hero (Step 6), 5 hôtels réels Tromso vérifiés multi-sources (Clarion The Edge, Scandic Ishavshotel, Radisson Blu, Thon Polar, Comfort Xpress), conversion NOK→EUR appliquée sur priceFrom ET petFee (÷11.5), photos hôtels+places, build PASS du 1er coup, audit 1→0 erreurs. Piège évité : `npm run build` lance `translate-to-pt.py` qui a modifié 5 city-guides ÉTRANGERS (gdynia/jurmala/kaunas/liepaja/regensburg.json) avec du PT Spanglish mécanique de mauvaise qualité — NE PAS les committer. | (1) Au démarrage, un working tree non-clean avec un orphan partiel = opportunité fast-track (≈50% du coût). Vérifier le blocker réel via `audit-destinations.mjs` : heroImage manquant = bloquant, 0 hotels = warning seul. Si seul le hero manque, c'est 5 min de travail. (2) Après `npm run build`, `git status` montre souvent des city-guides ÉTRANGERS modifiés par translate-to-pt.py (backfill PT idempotent, qualité Spanglish médiocre). TOUJOURS stager chirurgicalement (`git add` fichier-par-fichier, jamais `git add data/`) et `git restore` ces JSONs étrangers après le push pour laisser un tree propre. (3) Sight names Tromso ("Arctic Cathedral", "Fjellheisen Cable Car", "Telegrafbukta Park") = proper-noun + mot-catégorie, MÊME convention que le voisin Trondheim ("Nidaros Cathedral", "Kristiansten Fortress") — check-i18n ne bloque QUE les locutions touristiques génériques (Off-Leash Area, Riverside Walk…), pas les noms propres + Cathedral/Park/Fortress. (4) Pour les villes norvégiennes : mentionner la leash law nationale (1er avril–20 août, protection faune nidifiante) ; prix Booking en NOK à convertir ÷11.5 en EUR avant écriture. |
| 2026-06-08 | Daily run : choix de COMPLÉTER 2 villes déjà shippées mais à 0 hotels (Sorrento + Calais) plutôt que créer 2 nouvelles villes — fast-track cityContent+hotels, parité 43→39 warnings. DÉCOUVERTE GRAVE : les vets des city-guides créés par des runs précédents contiennent des NUMÉROS DE TÉLÉPHONE FABRIQUÉS. calais.json avait 3 vets avec des numéros à motif séquentiel évident (`+33 3 21 96 12 34`, `+33 3 21 34 56 78`, `+33 3 20 04 87 87`) + adresse/site CHV inventés ; ces faux numéros étaient répétés dans tips ET faqs (4 langues). sorrento.json avait "Clinica Veterinaria Sorrento +39 081 807 3245" non-vérifiable (la vraie clinique Via degli Aranci est "Ambulatorio Veterinario Gli Amici del Cuore", 081 362 2147). J'avais déjà propagé le faux numéro Sorrento dans cityContent.practicalInfo en début de run AVANT de le détecter → corrigé et re-pushé. Viole la RÈGLE IMMUABLE n°1 (jamais inventer un numéro de tél, vet phone = site officiel uniquement). | (1) **AVANT d'utiliser/propager TOUTE donnée vet d'un city-guide existant, VÉRIFIER le numéro** : un motif séquentiel/répétitif (`12 34`, `34 56 78`, `87 87`, `00 00`) = quasi-certainement fabriqué. Croiser nom+adresse+téléphone sur PagesJaunes / site officiel / annuaire vétérinaire avant de l'écrire dans cityContent. (2) Si non-vérifiable depuis ≥2 sources ou 1 source officielle → REMPLACER par une clinique réelle vérifiée, ne JAMAIS recopier. Sources fiables FR : PagesJaunes, vetosalines.com (Calais), numéro national de garde **3115**, Maison des Urgences Vétérinaires. Sources IT : PagineGialle, ospedale veterinario universitario H24. (3) Le problème est SYSTÉMIQUE sur tous les guides créés par les runs nocturnes (dunkerque, boulogne-sur-mer, sète, et probablement les ~25 autres villes 0-hotels + d'autres) → audit dédié nécessaire (spawn_task créé). (4) Quand on shippe une ville héritée (fast-track), RÉÉCRIRE la section vets avec données vérifiées AVANT commit fait partie du « shippable », au même titre que le build PASS. |
| 2026-06-13 | Daily run : working tree au démarrage contenait un GROS photo-backfill non-commité d'un run précédent (40 city-guides `data/city-guides/*.json` avec champ `photo` ajouté/renommé + 694 photos untracked dans `public/images/city-places/`). Stratégie choisie : COMPLÉTER 2 villes 0-hotels au guide déjà complet (Freiburg DE + Bayonne FR) plutôt que créer 2 nouvelles villes. Toruń envisagée mais REJETÉE : sa section vets avait un MAX-VET `+48 56 654 70 70` fabriqué (le vrai MAX-VET Toruń est ul. Wyspiańskiego 19, `56 651 85 74`) + section transport vide → trop de remédiation. Piège commun aux 2 villes shippées : les places `petsitting` (Rover/Pawshake/Gudog/Animaute) avaient un champ `photo` pointant vers des fichiers INEXISTANTS (`freiburg-petsitting-rover.jpg` etc.), jamais générés car le script fetch skip petsitting (lesson 2026-04-24). Corrigé en mettant `photo:""` (le front gère `!place.photo`). 185/232 parity-compliant (83→81 warnings). | (1) Au démarrage, si le working tree a un backfill non-commité d'un autre run (beaucoup de city-guides + photos untracked), NE PAS le committer : stager UNIQUEMENT mes 2 villes fichier-par-fichier (`git add data/hotels.json data/city-guides/{ma-ville}.json public/images/hotels/{ville}-*.jpg public/images/city-places/{ville}-*.jpg`), jamais `git add data/`. (2) Compléter une ville 0-hotels au guide complet = fast-track : juste 5 hôtels vérifiés + photos, et VÉRIFIER les vets existants (phone null = OK pas de fabrication, motif séquentiel = fabriqué → rejeter la ville ou réécrire). (3) AVANT de committer une ville héritée, lancer `python3 -c "import json,os; g=json.load(open('data/city-guides/{ville}.json'))['guides']; print([p['photo'] for s in g.values() for p in s.get('places',[]) if p.get('photo') and not os.path.exists('public'+p['photo'])])"` → toute sortie non-vide = photo cassée à mettre à `""` (typiquement petsitting). (4) `petFee:0` est la convention "upon request"/gratuit (222 hôtels). (5) `petPolicyPt` PAS requis sur les nouveaux hôtels (270 hôtels ne l'ont pas, translate-to-pt.py le backfill au build). |
| 2026-06-14 | Daily run : complété 2 villes 0-hotels au guide déjà complet (Poznań PL + Regensburg DE), fast-track « only hotels needed » (cityContent + guide déjà présents). Working tree au démarrage avait encore le GROS photo-backfill non-commité (658 photos city-places untracked + 37 city-guides modifiés `photo`) — stagé chirurgicalement mes 2 villes uniquement, jamais committé le backfill. CONFIRMATION du problème systémique de vets fabriqués (lessons 2026-06-08/06-13) sur les villes générées par les runs nocturnes : Poznań avait « Klinika Weterynaryjna Mokotów » (Mokotów = quartier de VARSOVIE, pas Poznań !) + « CZMZ +48 61 825 25 25 » (motif répétitif) ; Regensburg avait « Tierklinik Regensburg, Im Gewerbepark D 90, +49 941 4646464 » (adresse ET tél fabriqués — le vrai est Die Tierklinik Regensburg, Merianweg 3, 93051, +49 941 946 50 50, Dr Uwe Romberger) propagé jusque dans cityContent.ts practicalInfo×4 langues. Les 2 villes RÉÉCRITES avec 3 cliniques vérifiées chacune (sites officiels live) avant commit. | (1) Les villes 0-hotels « only hotels needed » (cityContent + guide déjà là) sont le fast-track le plus rapide : `node scripts/audit-destinations.mjs | sed -n '/Warnings only/,/TOTALS/p' | awk '/\(0 hotels\)/{c=$1;cc=0}/no cityContent/{cc=1}/0 hotels in/{if(cc==0)print c}'` liste les candidats. (2) MAIS leur section vets est presque toujours fabriquée → AVANT de shipper, réécrire les 3 vets avec des cliniques vérifiées live (WebFetch du site officiel) ET corriger toute propagation dans cityContent.ts practicalInfo (`grep -n "Tierklinik\|Klinik\|clinic" lib/cityContent.ts` autour du bloc ville). Red flags fabrication : nom d'une autre ville (Mokotów→Varsovie), tél répétitif/séquentiel (25 25 25, 4646464, 561234), adresse type « Im Gewerbepark » générique. (3) `priceFrom` ET `petFee` TOUJOURS en EUR avec `currency:"EUR"` — les composants (NearbyHotelCard, TopHotelsStrip, home/TopHotels, [guide]/page) hardcodent `€{priceFrom}`, seul HotelCard/HotelRankedCard respectent `currency`. Convertir PLN÷4.3, NOK÷11.5, CZK÷25, USD÷1.08, CAD÷1.45 avant écriture. Note : d'anciennes entrées (gdansk-1) ont `currency:"PLN"` + valeur PLN — bug d'affichage hérité, ne pas reproduire. (4) Le script `fetch-city-guide-photos.mjs` slugifie le `photo` par nom et RÉÉCRIT le champ dans le JSON → pas besoin de deviner le nom de fichier, lancer le script après avoir réécrit les vets et il aligne tout. |
| 2026-06-10 | Villefranche-sur-Mer shippée `46029b9` avec 3 numéros vétos fabriqués/erronés : (a) Clinique Vét. de Villefranche `+33 4 93 01 71 71` au lieu du vrai `+33 4 93 01 74 22`, adresse "8 Av. Sadi Carnot" au lieu de "Résid. Marco Polo, 9 Av. Albert 1er" ; (b) "CHV Saint-Martin" entry ENTIÈRE bogus : ce centre hospitalier est en Haute-Savoie (74370 Saint-Martin-Bellevue), PAS en Alpes-Maritimes — confusion avec "Clinique Val d'Azur - Dr Martin" à Saint-Laurent-du-Var ; (c) Azurvet `+33 4 22 13 20 20` au lieu du vrai `+33 4 97 10 07 10`. Cause racine : sub-agent shippé SANS outil WebSearch chargé (a flaggé "no fresh WebSearch verification" dans son résumé). 17 occurrences de "CHV Saint-Martin" disséminées dans intros/tips/emergencyHours×4 langues. | (1) AVANT de lancer un sub-agent qui doit faire de la véto/data verification, CHECK que WebSearch est dans son toolset effectif (sub-agent par défaut peut ne pas hériter de tous les outils du parent). Si pas certain, faire la verif vet inline dans le main avant/après le sub-agent. (2) Méfiance MAX sur les "centres référés" nommés "CHV/Clinique X" : vérifier que le centre cité est BIEN dans la région prétendue (CHV Saint-Martin → 74, pas 06 ; CHV Frégis → 94 ; CHV Pommery → 51). Un préfixe ville-département dans le nom n'implique pas la localisation réelle. (3) Pour la Côte d'Azur 24/7 référé : Clinique Val d'Azur (Saint-Laurent-du-Var, 04 97 12 02 68) ou Azurvet (Saint-Laurent-du-Var, 04 97 10 07 10) — pas "CHV Saint-Martin". (4) Quand un sub-agent flag dans son rapport qu'il a écrit qqch sans verif fraîche, traiter ça comme un bug bloquant : faire la verif inline et patcher AVANT de continuer. |
| 2026-06-14 | Locarno shippé `4a412fd` avec 3 vet entries entièrement fabriqués : Studio Veterinario Locarno (faux nom+adresse+phone), Clinica Veterinaria Tre Valli (vrai nom = Centro Veterinario 3 Valli Sagl, vrai lieu = Pollegio pas Bellinzona, phone faux), Ticino Veterinary Emergency (entité inexistante). Sub-agent a explicitement flaggé qu'il avait shippé sans WebSearch loaded. | Si un sub-agent flag "shipped without WebSearch verification" dans son rapport, NE PAS continuer — fix inline ou délègue rewrite avant la ville suivante. Le shoot-and-fix-later coûte 3× le temps du verify-first. |
| 2026-06-XX | **CATASTROPHE REVENUE** : 50+ city guides shippés sans jamais vérifier que les hôtels étaient listés. User screenshots Groningen montrent "No hotels listed yet for this combination" + photos manquantes ("?" placeholders) sur les place cards. Audit révèle **40/258 destinations avec 0 hôtel** (16% du catalogue !), couvre mes ships récents (leuven/ostend/groningen/leiden/delft/locarno/grindelwald/st-moritz/montreux/interlaken/balearic/ibiza/CB/SI/HR/GR/NL+coast/sitges-tavira-ericeira-nazare-tarifa…) MAIS AUSSI villes antérieures (gdynia, jurmala, kaunas, klaipeda, liepaja, klagenfurt, sankt-polten, villach, brest, vannes…). Problème systémique = chaque ship était un commit "city guide" sans backfill hôtels obligatoire. **Stay22 hotel strip vide = aucune conversion possible = 100% du trafic SEO perdu côté revenue.** Le script `scripts/audit-destinations.mjs` EXISTE et flag `<3 hôtels comme warning` mais je ne l'ai jamais lancé après mes ships. | (1) **Workflow ship complet OBLIGATOIRE** = JSON + destinations.json + editorial.ts + hero photo + **place photos (`node scripts/fetch-city-guide-photos.mjs --city={slug}`)** + **5 hôtels vérifiés Booking.com dans data/hotels.json** + validate + commit. PAS de ship sans les 5 hôtels. (2) **Après chaque batch de ship**, lancer `node scripts/audit-destinations.mjs` et fix toute ville en warning 0-hotels AVANT de passer au batch suivant. (3) Le coût ship sans hôtels = 0 € en SEO mais 0 € en revenue. Le coût ship AVEC hôtels = +5 min de WebSearch par ville mais des conversions Booking actuelles. (4) **Le script audit-destinations.mjs définit le contrat shippable** : heroImage (erreur bloquante), 8 sections guide (erreur bloquante), 5 hôtels (warning mais REVENUE-CRITICAL — traiter comme bloquant désormais). (5) **Mettre à jour le prompt sub-agent ship** pour inclure step "add 5 verified Booking.com hotels to data/hotels.json" comme step OBLIGATOIRE entre validate et commit. |
| 2026-06-15 | Daily run : shippé Haarlem (NL) + Sintra (PT) en **fast-track « only cityContent needed »**. Le backlog 0-hotels (lesson 48) a été résorbé par un run précédent : l'audit ne montre plus AUCUNE ville à 0 hôtel, les 63 warnings restants sont tous `no cityContent (history/sights/tips) entry` — des villes qui ont déjà guide complet (8 sections) + 5 hôtels + hero mais pas d'entrée dans `lib/cityContent.ts`. Compléter le cityContent = clore le dernier warning et atteindre la parité Amsterdam. Coût ≈ 1 fichier touché (lib/cityContent.ts), build PASS du 1er coup pour les deux. Working tree au démarrage : encore le GROS photo-backfill non-commité (19 city-guides modifiés `photo` + ~280 photos hôtels untracked) — stagé UNIQUEMENT `lib/cityContent.ts` fichier-par-fichier, jamais committé le backfill. PIÈGE VET CONFIRMÉ (lessons 06-08/13/14) : le guide `sintra.json` avait `Hospital Veterinário de Sintra +351 219 162 030` **non-vérifiable** — numéro suspect car quasi-transposition du vrai (`Centro Veterinário Sintra, R. Casal da Ligeira 64 Terrugem, +351 219 612 282`, 24h/365 vérifié sur veterinariosintra.pt : 162 vs 612 = chiffres permutés). J'ai mis le numéro VÉRIFIÉ dans practicalInfo, pas celui du guide. Haarlem : vet Caressa Dierenziekenhuis (Generaal Spoorlaan 89, +31 23 537 0220, 24h) vérifié OK = données guide fiables. | (1) Quand l'audit ne montre que des warnings `no cityContent`, le fast-track le plus rentable n'est plus « add hotels » mais « add cityContent » : 1 seul fichier (`lib/cityContent.ts`), insérer alphabétiquement le bloc {history×4, sights[6]×desc×4, petTips×4, practicalInfo×4}. Cibler des villes EUROPÉENNES bien documentées (skip boston/chicago/miami/los-angeles/san-francisco présents dans la liste warnings). (2) **TOUJOURS vérifier le vet AVANT de l'écrire dans practicalInfo** : ne JAMAIS recopier le numéro du guide hérité sans contrôle. Red flag = transposition de chiffres proche d'un vrai numéro (162↔612). Croiser sur le site officiel de la clinique. Si le guide a un n° vérifiable, OK ; sinon, remplacer par une clinique 24h réelle vérifiée. (3) Sight `name` = nom propre local pur (Molen De Adriaan, Castelo dos Mouros, Frans Hals Museum) — éviter descripteurs anglais (« windmill », « dog beach » → utiliser le terme local hondenstrand/molen), check-i18n valide 776 noms au build. (4) Backticks systématiques dans cityContent.ts, em-dash check `awk '/^  {slug}: \{/,/^  {next}: \{/' | grep -c "—"` = 0 avant build. |
| 2026-06-XX | **BUILD CRASH PROD** 7 deploys Vercel failed back-to-back depuis f2f994e jusqu'à 29368ae : tous les batches hôtels backfill ont produit des entrées sans les champs `slug` (string requis) et `highlights` (string[] requis) du type `Hotel` dans `components/HotelRankedCard.tsx`. 225 hôtels en prod cassaient le type check TypeScript sur `app/[locale]/[destination]/[category]/page.tsx:470`. Reproduit le pattern Poznań 2026-06-04 (string vs array) à plus grande échelle. Cause racine : j'ai sorti le schéma "hotel" des entrées existantes (id, name, destinationSlug, categories, stars, rating, reviewCount, priceFrom, currency, bookingUrl, petFee, petPolicy, address, neighborhood) SANS regarder le type TypeScript canonique défini dans `components/HotelRankedCard.tsx`. Toutes les entrées existantes (1196) avaient slug+highlights — les nouvelles 225 non. | (1) **AVANT** de batcher de l'ajout de données structurées, lire le type TypeScript canonique (grep `^interface\|^type` pour le model touché) ET pas seulement un échantillon JSON. Le JSON peut omettre des champs présents partout ailleurs. (2) **APRÈS** tout backfill > 5 entrées, lancer `npx tsc --noEmit` AVANT le premier push. Le check existe déjà dans le ship workflow mais je l'ai sauté pour les batches hôtels parce que "c'est juste de la data JSON". (3) Pour data/hotels.json spécifiquement, schéma obligatoire = `{id, slug, name, destinationSlug, categories[], stars, rating, reviewCount, priceFrom, currency, bookingUrl, petFee, petPolicy, address, neighborhood, highlights[]}`. (4) Mettre `npx tsc --noEmit` dans tout prompt sub-agent backfill, pas juste les ships nouvelle ville. |
| 2026-06-15 | `scripts/translate-to-pt.py` (regex ES→PT auto-translator qui tourne à chaque `npm run build`) produisait du Spanglish massif (258/264 villes flaggées) ET corrompait activement le PT natif existant : son pass "restore digits from ES siblings" a transformé "1,5 milhas" en "2,5 milhas" sur edinburgh quand je l'ai lancé. Le PT machine était indexé par Google qui choisissait un autre canonical (alerte GSC "Page en double" sur 207 URLs /pt/). | (1) Le PT (comme EN/FR/ES) est écrit NATIVEMENT au ship time par l'agent qui crée la ville — JAMAIS auto-généré par regex. (2) `translate-to-pt.py` a été remplacé par une GARDE : elle ne modifie aucun fichier, scanne les champs `*Pt` manquants (où `*Es` existe mais `*Pt` absent/vide) et fait `sys.exit(1)` avec la liste des gaps si du PT manque — bloque le build au lieu de shipper du Spanglish. (3) Toute nouvelle ville doit avoir EN+FR+ES+PT complets avant commit ; sinon le build échoue avec un message actionnable. (4) Ne JAMAIS committer l'output de l'ancien translate-to-pt.py (working-tree noise) — c'était du Spanglish/digits corrompus ; reverter ces fichiers. (5) Détecteur Spanglish : ` está ` et ` permite` sont identiques ES/PT (verbes estar/permitir) — NE PAS les mettre dans la blocklist, ils causent des faux positifs massifs. Marqueurs ES-only valides : están/tiene/tienen/hay/puede/pueden/permiten/ofrece/cuenta con/se encuentra/ciudad/aeropuerto/playa/perros/iglesia. |
| 2026-06-15 | La retraduction PT par sous-agents a introduit des bugs de FORME (pas juste de contenu) qui ont cassé le prerender Vercel : (1) tarragona/beaches avait `tipsPt` contenant des objets faqs `{q,a}` et `introPt` contenant le tableau des tips (décalage en cascade) → "Objects are not valid as a React child (found: object with keys {q, a})" sur /pt/.../beaches ; (2) 14 `languagesPt` étaient des strings au lieu de list (re-occurrence du bug Poznań 2026-06-04) → `place.languages.join()` throw. Le build i18n-check et la garde PT passaient (ils vérifient présence/Spanglish, PAS la forme). | (1) Après TOUT batch d'édition de données par sous-agents (traduction, backfill), lancer un check de FORME exhaustif : pour chaque champ `*Pt`, comparer son type (et type des éléments de liste) à son sibling EN/base sur les 264 fichiers. Mismatch str↔list ou list[str]↔list[dict] = bug runtime garanti. Script : walk récursif, `shape(pt) == shape(en)`. (2) Les agents qui réécrivent des arrays/objets peuvent décaler le contenu entre champs (intro↔tips↔faqs) ou aplatir un array en string — la validation Spanglish ne le voit pas. (3) `languagesPt` (et tout champ array-typé : amenities, services, tips, categories) DOIT rester une liste JSON. Bug déjà loggé 2026-06-04, ré-apparu via agents PT → ajouter le shape-check au pipeline post-batch systématiquement. (4) Un build qui prerender 9966 pages échoue sur la 1ère erreur (fail-fast) ; ne pas supposer qu'une seule page est touchée — faire le check global avant de re-push. |
| 2026-06-17 | Daily run : complété 2 villes en fast-track « only cityContent needed » (Matera IT + Siena IT), 62→60 warnings. Working tree au démarrage contenait encore le GROS photo-backfill hôtels non-commité (~280 photos `public/images/hotels/*.jpg` untracked + `scripts/_hotel-photo-failures.json` modifié + `scripts/_apify-hotels-groningen.json`) d'un run précédent — jamais committé en bloc. Constat utile : les photos hôtels de MES 2 villes (siena-1..5, matera-1..5) étaient dans ce backfill (untracked = absentes d'origin = cartes hôtels sans image en prod), toutes uniques (md5 distincts) et bien compressées (43–135 KB) → stagées chirurgicalement avec `lib/cityContent.ts` (11 fichiers exactement, vérifiés via `git diff --cached --name-only`). Vets vérifiés live AVANT écriture dans practicalInfo/petTips : Siena = Clinica Veterinaria Senese (0577 41412) + San Martino (0577 319102), Matera = Vetland (333 6989063) + Croce Blu (0835 262584, listée « Croce Blu 24h ») — les 4 confirmés sur PagineGialle/sites officiels, aucune fabrication. Build PASS 1er coup, 0 churn de fichiers étrangers (la garde PT ne modifie plus rien, lesson 06-15). | (1) Quand l'audit ne montre que des warnings `no cityContent`, fast-track = 1 fichier `lib/cityContent.ts` (history×4, sights[6]×desc×4, petTips×4, practicalInfo×4), insérer à la position alpha exacte. (2) Si le backfill hôtels d'un autre run est dans le working tree, les photos de MES villes sont récupérables : vérifier md5 unique + taille puis les stager fichier-par-fichier avec le cityContent (jamais `git add public/images/hotels/` en bloc). (3) En-dash « – » (U+2013, ranges de température/dates « 27–32 °C », « avril–mai ») est OK et cohérent avec le code existant ; SEUL l'em-dash « — » (U+2014) est interdit — le check est `grep -c "—"` = 0 par bloc, pas le en-dash. (4) Sights italiens : `name` = nom propre pur (Sasso Caveoso, Piazza del Campo, Fortezza Medicea, Orto de' Pecci), descripteurs en `desc`, check-i18n passe. (5) `Orto de' Pecci` contient une apostrophe → string en double-quotes `"Orto de' Pecci"` dans le champ `name` (pas backtick nécessaire car pas d'interpolation), le reste des strings cityContent en backticks. |
| 2026-06-15 | 3 bugs prod découverts ensemble : (1) IMAGES HÔTELS MANQUANTES — 361 photos d'hôtels existaient en local (`public/images/hotels/{id}.jpg`, valides) mais étaient UNTRACKED (jamais `git add`) → "?" sur prod pour ~70 villes backfillées (alcudia etc.). Le backfill hôtels avait ajouté les entrées hotels.json + fetché les photos localement mais sans les committer. (2) DROPDOWN HERO buggé — `<datalist>` HTML natif (components/home/Hero.tsx) rendu de façon imprévisible (liste détachée en haut-gauche au chargement). (3) IndexNow GH Action en échec — transitoire : le workflow ping-ait pendant que le build Vercel échouait, sitemap momentanément en erreur, `res.ok` false → exit 1 → email d'échec. | (1) Après tout fetch d'images (hôtels/lieux/destinations), TOUJOURS `git status public/images/` et committer les untracked AVANT de considérer le travail fini — une image en local ≠ une image en prod. La card hôtel lit `/images/hotels/${hotel.id}.jpg` (par ID, pas slug) ; auditer avec `existsSync` par id. (2) Ne pas utiliser `<datalist>` natif pour un autocomplete (rendu/position non fiables) — préférer un dropdown React contrôlé (state focus + query, position absolute, fermeture au blur). (3) Tout script de workflow GH qui dépend d'une ressource déployée (sitemap, API live) doit RETRY avec backoff — un build qui échoue ou un deploy lent ne doit pas faire échouer le job et spammer des emails. (4) Quand l'utilisateur signale "une page" avec un bug visuel, présumer que c'est SYSTÉMIQUE et auditer TOUT le catalogue (ici 264 destinations × hôtels), pas juste la page signalée. |
| 2026-06-18 | Analyse conversion (levier #2) : les pages /beaches de destinations PREMIUM (Lugano, Annecy) génèrent beaucoup de clics affiliés Stay22 (Lugano 14, Annecy 13 sur 14j) mais 0 booking. Diagnostic Clarity : pages techniquement OK (0 rage/dead click, 35-40s d'engagement), lien Stay22 bien ciblé sur le village de la plage, drop APRÈS le clic sur Booking. Cause double : (1) destinations lac premium (hôtels €175-475, 2 palaces 5* à €425/€475) = choc-prix vs côte méditerranéenne qui convertit (Tarifa/Barcelona/Nice) ; (2) intention day-trip (excursionnistes locaux, 100% bounce mono-page, Suisse-mobile dans le mix), pas séjour. Aggravant côté code : le tri hôtels était `rating desc` (TopHotelsStrip) ou AUCUN tri (category page = ordre id) → on menait avec le luxe €475 = choc-prix maximal au premier coup d'œil. | (1) Distinguer pages SEO-trafic (lacs premium = aimants organic, à garder) des pages REVENUE (côtes vacances = bookings). Ne pas juger une page sur sa conversion seule : Lugano nourrit l'autorité du domaine même sans booker. (2) Biais catalogue : shipper des CÔTES DE VACANCES (Costa Brava, Baléares, Algarve) > des lacs premium pour le revenu. (3) Tri hôtels = `valueSort` (lib/hotelSort.ts) : score = rating - (prix/prix_max_ville)×1.5. Mène avec une option abordable bien notée, démote le luxe extrême, ne scramble pas les côtes (prix serrés). Appliqué aux 4 surfaces (category page, guide page nearby, TopHotelsStrip) — elles étaient incohérentes (rating-desc / no-sort). (4) Quand une page a du trafic mais 0 conversion, vérifier Clarity (engagement vs bounce, rage/dead clicks) AVANT de présumer un bug : souvent c'est structurel (prix/intention), pas technique. |
| 2026-06-19 | Daily run Arcachon (full pipeline) : pipeline propre, build tsc PASS, PT guard OK, 0 em-dashes. Piège évité : `data/hotels.json` et `data/destinations.json` N'étaient PAS triés globalement dans HEAD (les villes sont juste append au fil du temps, ordre non-alpha cross-city : hamburg→faro existe). Mon `d.sort()` Python a re-trié TOUT le fichier hotels → diff monstre 778 ins/524 del qui réordonnait toutes les autres villes. Aussi : le working tree contenait déjà des orphans non-commités d'un run précédent abandonné (la-rochelle + ile-de-re dans destinations.json/hotels.json, + 5 city-guides untracked gran-canaria/lanzarote/tenerife/ile-de-re/la-rochelle). Mon sort a embarqué la-rochelle dans mon diff hotels. | (1) Pour `data/hotels.json` et `data/destinations.json`, NE PAS faire `d.sort()` global aveugle : ces fichiers ne sont PAS triés alpha cross-city dans le repo. Procédure surgicale : reconstruire `out = json.loads(git show HEAD:data/hotels.json) + mes_5_hotels` (HEAD intact + append uniquement mes nouvelles entrées), vérifier `added == mes ids` et `existing changed == 0` via diff Python contre HEAD. destinations.json EST trié donc re-sort OK, mais filtrer les orphans : `out = [h for h in HEAD if slug != moi] + [mon_entry]` puis sort. (2) Toujours diff contre `git show HEAD:file` (pas le working tree) pour détecter les orphans d'un run précédent et les exclure du stage. (3) Vets Arcachon vérifiés WebSearch : Arcavet 131 Bd Deganne 05 56 83 27 71, Clinique de l'Hippocampe La Teste (garde Bassin), 3115 national 24/7. Hôtels vérifiés : Ville d'Hiver (dogs free → dogs-stay-free), Les Vagues 5★, Grand Hôtel Richelieu, Best Western Le Port (≤10kg 12€), Le Dauphin. |

[2026-06-19] | A racing ship agent committed an orphan cityContent.ts block for an unshipped city (Tenerife) whose sight name held an English descriptor; check-i18n.mjs failed and every Vercel deploy fast-failed at ~36s | After any multi-agent ship, run `node scripts/check-i18n.mjs` locally before trusting the deploy, and grep cityContent.ts for blocks of cities not in destinations.json.
[2026-06-19] | Hotel card images (/images/hotels/{id}.jpg) for already-live cities (oviedo, dubai, abu-dhabi, arcachon, ras-al-khaimah, ile-de-re) were generated on disk but never git-added, so prod served fallback placeholders | After every ship, run `git status --short public/images/hotels/` and commit any untracked {id}.jpg for live cities. `node scripts/download-missing-hotel-images.mjs` fills gaps (picsum) and prints coverage.

[2026-06-19] | Sub-agents writing hotels.json packages put petFee as a descriptive string ("Confirm with property", "EUR 25 per night") instead of a number, which passes JSON validation + check-i18n + PT guard but crashes `tsc` at TopHotelsStrip.tsx (petFee:number) | The wiring step must coerce petFee to int before writing: parse first integer, 0 only on explicit "Free", else an indicative default (15). Re-apply the dogs-stay-free<->petFee===0 invariant after coercion. Always run `npx tsc --noEmit` before committing a batch.
[2026-06-20] | Daily run Tarifa (ES, Costa de la Luz) + Lagos (PT, Algarve) en fast-track « only cityContent needed », 78→76 warnings, build PASS 1er coup, 0 churn de fichiers étrangers (PT guard ne modifie plus rien). PIÈGE VET CONFIRMÉ (lessons 06-08/13/14/15) : le guide lagos.json avait 3 vets NON-VÉRIFIABLES — « Hospital Veterinário Mãe d'Água, Estômbar, +351 282 419 080 » (introuvable sur le web, probablement fabriqué), « Clínica Veterinária de Lagos, Av. dos Descobrimentos 13, +351 282 762 901 » (adresse/tél ne correspondent à AUCUNE clinique réelle de Lagos), « Hospital Veterinário do Algarve Faro +351 289 070 333 » (le vrai 24h de Faro est AniCura Algarve, +351 289 806 047). N'AI RIEN propagé du guide ; practicalInfo écrit avec des vets VÉRIFIÉS LIVE : Hospital Veterinário de Portimão (+351 282 418 260, 24h, WebFetch site officiel) + Onevet Petvet Lagos (Rua Capitão Salgueiro Maia, +351 282 799 268, WebFetch). Tarifa : Hospital Veterinario Alba Algeciras (+34 956 667 352, 24h) vérifié WebSearch, matche le guide. | (1) Continuer à appliquer v5 strict pour le fast-track cityContent : 1 fichier lib/cityContent.ts, insertion à la position alpha exacte (tarifa avant tarragona, lagos avant lausanne), backticks systématiques, name de sights = nom propre local pur (Ponta da Piedade, Playa de Los Lances, Castillo de Guzmán el Bueno), 0 em-dash, history×4+sights[6]×desc×4+petTips×4+practicalInfo×4. (2) Pour TOUTE ville algarvienne/portugaise héritée, ne JAMAIS recopier les vets du guide sans WebFetch du site officiel — le pattern de fabrication systémique frappe encore (Mãe d'Água Lagos = introuvable). Vets fiables Algarve : Hospital Veterinário de Portimão (24h), AniCura Algarve Faro (24h), Onevet Petvet Lagos. (3) Tip différenciant Algarve/sud Portugal : prévention leishmaniose (phlébotome endémique) dans petTips ET practicalInfo. Tip différenciant Andalousie côtière/Tarifa : interdiction estivale des chiens sur plages surveillées + paperasse ferry Tanger (passeport UE/puce/rage pour le retour). |

[2026-06-20] | Hotel CARD images were random picsum.photos placeholders (water lily, surfer, coyote) unrelated to the hotel; Apify Google Places was only wired for destination heroes + city-place photos, never the hotel cards. User noticed the mismatch immediately. | Use scripts/fetch-hotel-photos-apify.mjs (NEW) to pull a real Google Places photo per hotel by id, matched on the hotel name's first distinctive token (rejects city-token-only false positives like "Galaxy Hotel Argostoli" -> "Argostoli Bay Apartments"); unmatched hotels fall back to a neutral placeholder, never a wrong-property photo. Run --slugs=a,b on every new batch after shipping.

[2026-06-20] | Vercel deploy failed at the "Deploying outputs..." phase (NOT the build, which passed) after a commit added ~20MB of full-size hotel photos; public/images had reached ~1GB and crossed the deployment-size ceiling. The real error was hidden by a "log length exceeded 4 MB" truncation, so the build looked fine. | When a deploy fails AFTER "Build Completed"/"Deploying outputs...", suspect total static-asset size, not code. Keep public/images well under ~1GB. Hotel cards render ~300px so encode hotel photos at 600x400 q68 (~30KB), heroes at 1200x800. The structural bloat is public/images/city-places (~732MB); compress it or move images to external hosting (Vercel Blob/CDN) rather than shipping them in the Git deploy.

[2026-06-21] | Daily run Nerja (ES, Costa del Sol) + Rovinj (HR, Istria) en fast-track « only cityContent needed », 93→ ~92 warnings (le -1 net masque -2 nerja/rovinj +1 lignano-sabbiadoro qui était un orphelin partiel committé en 7fc891b avec son guide JSON + photos laissés untracked dans le working tree). Build PASS 1er coup pour les deux, tsc + check-i18n + audit clean, 0 em-dash. PIÈGE BUILD-CHURN observé : après `npm run build`, `git status` montrait data/destinations.json, data/hotels.json, lib/editorial.ts en « M » alors que `git diff` était VIDE (mtime touché par le build, contenu identique) ; un `git diff --numstat` ultérieur confirmait 0 changement réel, seul lib/cityContent.ts avait du diff. Vets vérifiés LIVE avant écriture dans practicalInfo : Nerja = Clínica Veterinaria Nerja (C. El Chaparil 3, +34 952 521 401, urgences 24h 609 982 941, confirmé PaginasAmarillas/Animalia) ; Rovinj = le guide avait phone=null (pas de fabrication, OK) → trouvé les vrais : Veterinarska Ambulanta Rovinj (Laste ul. 9, +385 52 813 214, urgence 24/7 +385 98 334 092) + Istra Vet (Gripole 68, +385 52 553 907), confirmés mojkvart/eistra/vet-rovinj.com. | (1) Quand l'audit ne montre que des warnings `no cityContent`, fast-track = 1 fichier lib/cityContent.ts (history×4, sights[6]×desc×4, petTips×4, practicalInfo×4) inséré à la position alpha exacte (nerja avant nice, rovinj avant salamanca). Backticks systématiques pour TOUTES les strings y compris les tips (évite l'échappement \\' en FR/ES/PT). (2) Ne PAS s'alarmer du « M » sur destinations.json/hotels.json/editorial.ts après le build : vérifier avec `git diff --numstat` — si 0/0, c'est juste un touch mtime, stager UNIQUEMENT lib/cityContent.ts. (3) Les blocs es/pt des anciennes entrées cityContent.ts sont contaminés par du Spanglish (ère translate-to-pt.py) ; écrire du ES et du PT NATIFS propres pour les nouvelles villes (mieux que l'existant). (4) Tip différenciant Costa del Sol/Andalousie côtière : prévention leishmaniose (phlébotome endémique) + interdiction estivale chiens sur plages 1 juin–30 sept → rabattre sur la playa canina de Torrox. Tip différenciant Istrie/Croatie côtière : ville très dog-beached (4+ plages canines signalées dans/près du parc Punta Corrente), pas de train (Pula 35 km), catamaran saisonnier Venezia Lines vers Venise.

[2026-06-21] | The deploy-size ceiling recurred: 8374413 + its redeploy failed at "Deploying outputs..." at only 528MB of public/images, while the prior commit (f401554, 3 cities fewer) succeeded. The limit is on the TOTAL deployment output (.next build output + public/), not just images, and .next grows with every city (309 guides now), so the total creeps back to the ceiling each batch and the marginal city tips it over. Compressing public/images is only a temporary buffer. | Re-compress aggressively when near the cliff (hotels 600px q66, city-places 700px q66) for an immediate green, BUT the durable fix is to move images out of the Git deploy entirely (Vercel Blob / external bucket+CDN). Do that before continuing high-volume city shipping. A redeploy does NOT help a size-driven deploy failure.

[2026-06-21] | RESOLVED the recurring deploy-size ceiling: migrated public/images (6833 files, ~373MB) to Supabase Storage public bucket + CDN and removed them from the Git deploy. | Images now served via a custom next/image loader (lib/image-loader.js) that maps /images/* -> NEXT_PUBLIC_IMAGE_CDN (set in Vercel env), a manifest (data/image-manifest.json + lib/imageManifest.ts) replacing on-disk existsSync checks, imageUrl() for JSON-LD/og strings, and a /images/* -> CDN redirect. The Supabase JS SDK crashes on init (realtime-js) under Node 20 -> use the Storage REST API via fetch (scripts/upload-images-supabase.mjs). NEW-CITY WORKFLOW: after generating images run `npm run images:sync` (uploads new files + regenerates manifest) and COMMIT data/image-manifest.json (it now carries the "this photo exists" info since public/images is gitignored). Full runbook: tasks/supabase-image-migration.md.

[2026-06-22] | Daily run Santander (ES, Cantabria) + Pula (HR, Istria) en fast-track « only cityContent needed », 98→96 warnings, build PASS 1er coup pour les deux, check-i18n + audit clean, 0 em-dash, real diff = lib/cityContent.ts uniquement (data/destinations.json/hotels.json/editorial.ts montraient « M » par touch mtime du build mais `git diff --numstat` = 0/0, stagé uniquement cityContent.ts). VETS : Santander = données du guide FIABLES (Animals Cantabria, Av. de los Castros 73, +34 942 07 09 00 24h + Hospital Veterinario Cantabria Guarnizo urgences 24h +34 608 48 38 21, les deux confirmés sur sites officiels + PaginasAmarillas). Pula = guide avait phones=null (pas de fabrication) + une ERREUR dans l'intro : « national emergency dispatch is 192 » — 192 est la POLICE croate, pas un véto ; vrais véto trouvés/vérifiés : Veterinarska ambulanta Pula d.o.o. (Dinka Trinajstića 1, +385 52 541 100, mobile urgence +385 98 98 11 666, confirmé pula.hr + mojkvart) et Specijalistička veterinarska ambulanta Hajster (Marianijeva 3, +385 52 500 501, site officiel veterina-hajster.hr). N'AI PAS propagé le « 192 » ni la référence vague « Veterinarska klinika Buba (Istria region) ». | (1) Fast-track cityContent reste le plus rentable quand l'audit ne montre que des warnings `no cityContent` : 1 fichier, insertion alpha exacte (santander entre 'san-sebastian' et 'santiago-de-compostela' ; pula entre prague et reims), backticks systématiques, name de sights = nom propre local pur (Pulska Arena, Slavoluk Sergijevaca, Kaštel, Park prirode Kamenjak, Península de la Magdalena, Faro de Cabo Mayor). (2) Même quand le guide hérité a des phones=null (donc pas fabriqués), VÉRIFIER quand même le contenu de l'intro vets : Pula y affichait « 192 » comme numéro véto alors que c'est la police croate (général = 112). Ne jamais recopier un numéro d'urgence d'un guide sans le situer (police 192 / pompiers 193 / ambulance 194 / général 112 en Croatie). (3) Tip différenciant Croatie côtière : beach law nationale interdit les chiens sur les plages de baignade non désignées 15 mai–15 sep, rabattre sur les criques canines signalées (cap Kamenjak, section Ambrela). Tip différenciant côte nord Espagne/Cantabrie : interdiction estivale plages urbaines 1 mai–30 sep + plage canine de repli Loredo (Ribamontán al Mar) de l'autre côté de la baie + races PPP toujours laisse+muselière.

[2026-06-22] | picsum.photos (used by download-missing-hotel-images.mjs and the no-match fallback in fetch-hotel-photos-apify.mjs) intermittently fails/rate-limits, silently leaving hotel {id}.jpg files NOT created, so the manifest+CDN miss them and those cards lose their image. | Do not depend on picsum for placeholders. Generate neutral placeholders LOCALLY with sharp (an SVG gradient, no network) for any hotel id still missing after the Apify pass. Always verify per-city image counts (5 hotels each + 1 hero) on disk AND on the CDN before committing. The Supabase migration adds a mandatory `npm run images:sync` + commit data/image-manifest.json step.

[2026-06-23] | Daily run Saint-Malo (FR, Bretagne) + Cassis (FR, Provence) en fast-track « only cityContent needed », 105→103 warnings, build PASS 1er coup pour les deux, check-i18n + audit clean, 0 em-dash, real diff = lib/cityContent.ts uniquement. CONTRE-EXEMPLE utile au pattern vet-fabriqué : pour une fois les DEUX guides hérités avaient des vets RÉELS et vérifiables — Saint-Malo (La Rose des Vents +33 2 99 81 75 54 confirmé site officiel/PagesJaunes, urgence locale +33 2 99 41 16 46 confirmée veterinaires2touteurgence, 3115) et Cassis (Clinique du Messuguet/« Cabinet Vét. de Cassis » +33 4 42 01 15 30 confirmé PagesJaunes, CHV Massilia 121 av. de Saint-Julien Marseille +33 4 91 82 13 13 confirmé centre référent 3115 24h). Seule correction de nom : le guide Cassis disait « Cabinet Vétérinaire de Cassis », le vrai nom officiel est « Clinique vétérinaire du Messuguet » — j'ai utilisé le nom vérifié dans practicalInfo. | (1) Toujours vérifier les vets même quand ils SEMBLENT plausibles, mais ne pas présumer fabrication : ~1 ville sur 5 a des données guide fiables, le check live (WebSearch nom+adresse+tél) tranche en 1 requête. Red flag = motif séquentiel/répétitif ou nom d'une autre ville ; ici aucun, donc propagation directe après confirmation. (2) Une apostrophe typographique U+2019 dans un champ `name` de sight (« Presqu’île de Port-Miou ») passe check-i18n sans souci (ce n'est pas un descripteur anglais) ; pas besoin de la convertir, mais le reste des strings cityContent reste en apostrophe droite + backticks. (3) Tip différenciant Provence/Calanques : le CŒUR du Parc national des Calanques (Port-Pin, En-Vau, Sugiton) est interdit aux chiens MÊME EN LAISSE — erreur n°1 du visiteur — rabattre sur Port-Miou, cap Canaille/route des Crêtes, vignoble AOC ; + accès au massif réglementé l'été selon risque incendie (carte quotidienne préfecture). Tip différenciant Bretagne nord/Saint-Malo : interdiction TOTALE des chiens sur TOUTES les plages 1er avril–30 sept (amende 35 €), remparts/digues/GR34 ouverts en laisse toute l'année ; + marées jusqu'à 13 m de marnage qui isolent Grand Bé/Fort National.
[2026-07-04] | 5 deploys Vercel consecutifs en echec (buildStep: Maximum call stack size exceeded apres Build Completed) car 11 368 pages sections pre-generees ont depasse la limite de traitement de l'output Vercel | Si un deploy Vercel echoue apres un Build Completed OK, verifier le nombre de pages dans .next/prerender-manifest.json ; au-dela de ~10 000, passer la famille de routes dominante en ISR (generateStaticParams vide + dynamicParams + revalidate), pattern deja utilise par /hotels/[slug] et /destinations/[slug]/[guide]

[2026-07-10] | Daily run Cinque Terre (IT, area destination) + Positano (IT), 2 nouvelles villes full-parity, build/audit/i18n/contamination clean, 2 deploys Vercel success confirmes via gh commit status. DEUX pieges process : (1) translate-to-pt.py N'EST PLUS un traducteur ES->PT, c'est desormais un GUARD strict lance par `npm run build` (check via check-i18n) qui FAIL le build si un champ *Es d'un city-guide JSON n'a pas de sibling *Pt non-vide. Le task-file daily-city-guides.md dit encore (a tort) que PT est auto-backfille et « No manual PT step needed » : c'est FAUX depuis le nouveau guard : il faut ecrire le PT natif (pt-PT) dans le guide JSON au ship time, sinon `❌ PT GUARD FAILED: N missing PT field(s)`. (2) fetch-destination-photos-apify.mjs matche le hero par overlap du nom de ville dans le title du POI Google ; pour une destination de type AIRE/zone (« Cinque Terre »), aucun POI ne s'appelle « Cinque Terre » -> « no match » -> pas de hero -> erreur bloquante audit. | (1) Pour toute nouvelle ville, ecrire les 4 locales EN/FR/ES/PT natives DANS le city-guide JSON (approche efficace : ecrire EN/FR/ES via Edits sentinelle section-par-section, puis 1-2 scripts Python qui ajoutent les champs *Pt natifs par section, puis `python3 scripts/translate-to-pt.py {slug}` doit afficher « ✅ PT guard »). Le task-file est perime sur ce point. (2) Pour un hero de destination-aire (Cinque Terre, iles, massifs), le match par nom echoue : fetcher directement via un mini-script node inline reutilisant l'acteur compass~crawler-google-places avec une requete sur un POI ICONIQUE precis (« Manarola Cinque Terre », « Vernazza »), prendre la 1re imageUrls et sharp.resize(1200,800) -> public/images/destinations/{slug}.jpg. Idem pour les places abstraites (transport « By car & parking », bus/shuttle/ferry) que le fetch-city-place-photos rate : requete POI proche (gare, marina, village) + save au chemin exact. (3) scrape-booking.mjs N'ECRIT PAS le champ `slug` (schema Hotel le requiert, cf lesson build-crash) : backfiller `slug = slugify(name)+'-'+destinationSlug` sur les nouveaux hotels avant build, en re-dumpant hotels.json avec json.dumps(indent=2, ensure_ascii=False) pour matcher JSON.stringify (diff propre, 0 deletion). (4) public/images/ est gitignore : les images vivent dans le bucket Supabase via `npm run images:sync` (tracke dans data/image-manifest.json) ; ne PAS chercher a git add des images, committer data/image-manifest.json. (5) Angle chien honnete Cinque Terre = train regional + ferry Golfo dei Poeti (gratuit muselière+laisse, pont exterieur, PAS Corniglia) + Sentiero Azzurro/Via dell'Amore en laisse (Trekking Card) ; plages villages interdites. Positano = terrain vertical (centaines de marches), plages restreintes juin-sept, vrai highlight = Sentiero degli Dei (Bomerano->Nocelle en laisse), bus SITA + ferries Travelmar/Alicost muselière+laisse (>10kg au chenil du pont).

[2026-07-11] | Daily run Portofino (IT), full-parity new city, build/i18n/audit/contamination clean, Vercel deploy success confirmed via gh commit status (3d8fc4af). NEW PROCESS TRAP: scrape-booking.mjs wrote GARBAGE into the hotels' `petPolicy` field, not a pet policy but the scraped address + a marketing blurb containing an em-dash, e.g. "Calata Marconi n°25 int 1, 16034 Portofino, ItalyExcellent location — rated 9.8/10!(score from 78 reviews)Real guests • Real stays • Real opinions". That em-dash slipped past the JSON build (petPolicy is a free string) and would have violated the no-em-dash rule if committed; only caught by `git diff --cached | grep "^+" | grep -c "—"` before commit. Also confirms: scrape-booking.mjs still does NOT write `slug` (backfilled slug=slugify(name)+'-portofino'); priceFrom defaults to 120 EUR, petFee 0, highlights[] present, cat-friendly added by default. Hero via fetch-destination-photos-apify matched fine (Portofino is a named town, unlike Cinque Terre); it returned the Faro di Portofino promontory shot (on-theme, verified by eye). 14/18 place photos filled, the 4 misses were abstract POIs (a coastal walk, the ferry service, the 782 bus line, the car park) that Apify cannot match, front degrades gracefully. | (1) After scrape-booking.mjs, ALWAYS rewrite the new hotels' `petPolicy` to a clean sentence ("Dogs and cats welcome; please confirm conditions with the property when booking." for cat-friendly, "Dogs welcome; ..." otherwise) BEFORE staging, because the scraper stuffs address+review-blurb+em-dash into it. (2) Run `git diff --cached | grep "^+" | grep -c "—"` as a hard pre-commit gate, not just a per-file check on the content you authored, because scraped DATA (petPolicy, highlights) can carry em-dashes and bullets (•) you never typed. (3) The em-dash lesson (2026-05-31) applies to scraped fields too, not only prose you write. (4) Do NOT blind `d.sort()` destinations.json (Arcachon trap 2026-06-19 confirmed again): it is NOT globally alpha-sorted, sorting produced a 225/150 reorder diff; instead insert the new entry right before its alpha neighbor (portofino before positano) for a clean 75/0 diff.

[2026-07-19] | Daily run Sydney (AU), first Oceania-cluster city, full pipeline (guide 8 sections + destinations.json + editorial + cityContent + 8 Booking hotels + Apify hero/place photos + images:sync). Build/tsc/check-i18n/PT-guard/contamination all clean 1st pass, audit 0 errors 0 warnings across 373 destinations, 2059 prerender routes (well under the ~10000 Vercel ceiling). Process notes: (1) macOS has no `timeout` binary, do NOT wrap node scripts in `timeout 280 ...` (command not found, aborts the run), rely on the Bash tool's own timeout param instead. (2) First Python pass on editorial.ts stripped accents (wrote ES "ano" instead of "año" = anus vs year, and dropped FR/PT accents) because I over-escaped to ASCII, backtick TS strings take UTF-8 fine, always write full accents (reverted via git checkout + redid). (3) scrape-booking.mjs confirmed AGAIN stuffing address+review-blurb+em-dash+bullets into petPolicy and omitting slug/address/neighborhood, backfilled slug=slugify(name)-sydney, extracted address by splitting on "(Excellent|Very good|...) location", rewrote petPolicy to the clean cat+dog sentence, priceFrom defaulted to 120 EUR / petFee 0 / highlights present. (4) Verified non-sydney hotels.json entries byte-identical vs `git show HEAD` before commit (no blind sort). (5) Hero via fetch-destination-photos-apify matched Sydney Customs House at Circular Quay (genuine Sydney landmark, verified by eye, on-theme but not the Opera House/Bridge, acceptable). | (1) Never prefix node scripts with `timeout` on macOS. (2) Write native accented UTF-8 in backtick TS strings, never ASCII-fold, spot-check ES "año"/"niño"-type words. (3) Post-scrape hotel hygiene (slug backfill + petPolicy rewrite + em-dash gate (git diff --cached, filter added lines, grep -c for the U+2014 char)) is mandatory, the scraper output is dirty every time. (4) Honest Sydney dog angle: extremely dog-friendly for outdoor life (harbour dog beaches Rose Bay/Sirius Cove/Greenhills-Cronulla, huge off-leash reserves Sydney Park/Centennial/Rushcutters Bay, year-round terrace dining) BUT famous surf beaches (Bondi/Manly/Coogee) ban dogs entirely, off-leash is zone/hour-gated (fines ~AUD220), National Parks land bans dogs, public transport largely excludes pets (no trains/Metro; buses/ferries small carrier at staff discretion), paralysis tick (Ixodes holocyclus) is a fatal coastal/bush risk, and Australia import = strict biosecurity + min 10-day Mickleham quarantine. Verified 24/7 vets: SASH North Ryde (1 Richardson Pl, +61 2 9889 0289), SASH Alexandria (Gardeners Rd, +61 2 8913 4000), ARH Homebush (250 Parramatta Rd, +61 2 9758 8666).

[2026-07-23] | Daily run Bad Gastein (AT, Salzburg/Pongau), first Gastein-valley/Alpine-spa city, full pipeline (guide 8 sections + destinations.json + editorial 4 locales + cityContent + 8 Booking hotels + Apify hero/place photos + images:sync). Build/tsc/check-i18n/PT-guard all clean 1st pass, audit 374/375 fully parity-compliant (the lone error `olhao` is a pre-existing orphan, not mine), contamination scan 0 for bad-gastein (24 pre-existing others untouched). Vercel deploy success via gh commit status (6c07a7fe), IndexNow 29496 URLs HTTP 200. Process notes that held: (1) scrape-booking.mjs AGAIN stuffed garbage into petPolicy ("Sustainability certification", addresses) and omitted slug for all 8 hotels; backfilled slug=slugify(name)-bad-gastein, rewrote petPolicy to the clean cat+dog sentence, scrubbed em-dash/bullet from highlights, verified 2128 non-bad-gastein entries byte-identical vs HEAD before commit (196/1 additive diff). (2) The Apify place-photo fetcher missed 1/10 (the abstract Belle Epoque "town centre" POI, same class as Cinque Terre/transport misses); filled it with a one-off inline node script querying an ICONIC named POI ("Grand Hotel de l'Europe Bad Gastein") -> got a perfect Belle Epoque facade. NOTE: the inline script MUST be run from the repo dir (or copied there) so `import sharp` resolves; a scratchpad-dir copy fails ERR_MODULE_NOT_FOUND. (3) All 9 place photos + hero visually verified subject-correct (waterfall gorge hero, real vet operating, wood-fired pizza oven for Jagerhausl, Brettljause platter for Himmelwandhutte, the Prossau interior literally name-labelled, genuine Stubnerkogel suspension bridge). | (1) Post-scrape hotel hygiene (slug backfill + petPolicy rewrite + em-dash/bullet scrub + byte-identical check of untouched entries) is mandatory every run, scraper output is dirty every time. (2) For abstract/area POIs the Apify name-match misses, a one-off inline fetch on a specific iconic named landmark POI is the reliable fix; run it from the repo root for sharp to resolve. (3) Honest Bad Gastein dog angle: excellent Alpine spa base (gondolas carry dogs 5 EUR/day, flat Kotschachtal valley with 2 dog-friendly huts, 341 m town-centre waterfall + Wasserfallweg free, Kaiser-Wilhelm-Promenade) BUT thermal baths/radon gallery NEVER admit dogs (pet-specialist hotels e.g. GRUBERS Bockstein offer free dog-sitting during treatments), terrain is vertical (stairs everywhere), muzzle+leash compulsory on OBB trains + SVV buses, no bathing lake in the valley (beaches=coming-soon, nearest lake swim Zell am See ~40 km), Salzburg-province leash/muzzle rule in public, cattle on summer alpine pastures are the top dog-incident risk, waterfall path closes in winter. Verified vet: Tierarztpraxis Mag. Katrin Laubichler, Achenstrasse 17 Bad Hofgastein (7 km), +43 664 3840438, small-animal Mon/Wed/Fri 11-13 by phone appt; nights/weekends via Salzburg-province rotating on-call (phone practice first, 112 is human-only).

[2026-07-23] | Daily run Olhao (PT, eastern Algarve, beach-led). A CONCURRENT Bad Gastein session committed on top of my first olhao commit (916b816b) WHILE I was mid-run: commits 6c07a7fe + ffd32898 landed, and that session's `git add` swept in my then-uncommitted olhao working-tree edits PARTIALLY (editorial en/fr/es blocks + destinations.json entry got captured into the Bad Gastein commit, but my editorial PT block, my full cityContent olhao entry, my hotels and image-manifest did NOT). I nearly clobbered it: after building I did `git checkout HEAD -- editorial.ts cityContent.ts destinations.json image-manifest.json` to isolate my commit, which REVERTED those partial-olhao pieces to HEAD. Recovered by treating current HEAD as the baseline and re-adding only the MISSING olhao gaps (PT editorial block after the pt-locale olbia anchor, full cityContent entry before olomouc, regen manifest via images:sync), then a clean olhao-only commit (965de811), Vercel success via gh commit status. | (1) On a shared single-branch repo, ANOTHER session can commit between your commits; before any `git checkout HEAD -- <file>` to isolate, run `git log --oneline -8` and `git log -S'<yourslug>: {' -- <file>` to see if your own edits were already swept into someone else's commit. If so, do NOT checkout-revert; instead diff HEAD for your slug and fill only the gaps. (2) `git checkout HEAD -- file` silently discards uncommitted work in that file. Back up to /tmp first (I did, which saved me) OR verify the file has no wanted uncommitted content. (3) scrape-booking.mjs takes the slug as a POSITIONAL arg (`node scripts/scrape-booking.mjs olhao`), NOT `--slug=olhao`; passing `--slug=` makes it a flag, slugArgs is empty, and with no --missing/--low it defaults to ALL 375 destinations and starts re-scraping a-coruna, aarhus... Kill it fast and restore the clobbered early-alphabet slugs from `git show HEAD:data/hotels.json`. (4) Olhao honest angles: NO official dog beach in the concelho; national bathing-season ban 1 Jun-30 Sep on the 5 supervised beaches (Armona-Mar/Ria, Fuseta-Mar/Ria, Cavacos), off-season Oct-May leashed island walking is the real draw; scheduled island carreiras (Transportes do Guadiana) accept dogs ONLY in a carrier, big dogs need a private aqua-taxi (dogs on deck, ~30-50 EUR); Ria Formosa is a Natural Park (leash near dunes/sapal, bird protection). Verified vets: Centro Veterinario Porto Seguro (+351 289 707 303, urg +351 967 721 777), 115 Animal (+351 289 715 115), HVA Faro 24/7 (+351 289 880 100). (5) Apify wrong-subject images blanked: "Mercados de Olhao" returned a dog-park fountain, "Hospital Veterinario do Algarve" returned an AniCura storefront (different brand than the named entity), "Rover" petsitting returned a LAND ROVER car dealership. Always eyeball petsitting/brand-name POIs; generic brand names (Rover) mis-match badly.

[2026-07-24] | Daily run Yokohama (JP, next Japan-cluster city after tokyo/kyoto/osaka), full pipeline (guide 8 sections + destinations.json + editorial 4 locales + cityContent + 8 Booking hotels + Apify hero/place photos + images:sync). Build/tsc/check-i18n/PT-guard/contamination all clean, audit 0/0 across 377 destinations, hero verified (Minato Mirai Landmark Tower + Air Cabin), Vercel deploy success via gh (ec604a14), IndexNow 29836 URLs HTTP 200. CONCURRENT-SESSION COLLISION (worse than Faro/Cascais): a parallel maspalomas daily-run shared the SAME working dir + git repo and committed TWICE mid-run (4ae4cfa9). Its second commit ran its own scrape/slug-backfill that OVERWROTE my already-cleaned yokohama petPolicy with fresh DIRTY scraped values (address blurb plus an em-dash inside "Excellent location ... rated 9.3/10"), added slugs, and committed hotels.json before I committed my hygiene. Detected because git status showed my hotels.json edits gone + HEAD advanced. Fix: re-applied petPolicy/address hygiene on the committed-dirty entries, verified non-yokohama byte-identical vs HEAD + 0 em-dash on yokohama entries, committed forward (ec604a14). My earlier content commit 5b8143dd was swept into the concurrent run's push (already on origin). | (1) When another daily-run shares the working dir, re-check `git log --oneline -1` AND `git status` before EVERY commit: if HEAD moved and your staged data files (hotels.json, image-manifest.json) vanished from `git status`, the other run committed your uncommitted work, possibly in a DIRTY state (its scrape overwrote your hygiene). Re-verify the committed content, not just presence. (2) The em-dash gate MUST run on `git diff HEAD~n` or on the actual committed hotel entries, not only your pre-commit working copy, because a concurrent scrape can re-dirty petPolicy after your hygiene. (3) Commit hotels.json hygiene IMMEDIATELY after cleaning (do not defer through photos + images:sync + build), so a concurrent run cannot sweep the dirty pre-hygiene state into its commit. (4) Yokohama honest dog angle: compact walkable bayfront (Yamashita Park, Kishamichi, Marine & Walk terraces, Osanbashi whale-back rooftop) + one real off-leash run SHIBA to KAZE natural-grass at Rinko Park (scheduled dates, checks vaccination) BUT Japan import is the biggest hurdle (ISO chip + 2 rabies + antibody titre + 180-day wait + advance quarantine notice, start ~1yr ahead), trains/buses carrier-only, indoor dining/temples/Sankeien garden off-limits, no urban swimming beach (working port, beaches=coming-soon, nearest dog sand Shonan/Zushi ~1h). Verified vets: DVMs Animal Medical Center Yokohama night ER 20:00-03:00 (045-320-9099), Akatsuka Dog & Cat Hospital Tsurumi English-friendly (045-582-5575), Minato Yokohama Animal Hospital Isogo daytime (045-751-6310).
